# ai-ready

**코드베이스를 AI 에이전트가 다루기 좋은 상태로 만들고, 그 위에서 코드 변경을 무인으로 검증·빌드아웃하는 Claude Code 플러그인.**

세 가지를 하나의 플러그인으로 묶습니다.

1. **audit** — 코드베이스의 AI 준비도(AI-readiness)를 7개 카테고리 100점 척도로 채점하고 개선 로드맵을 만듭니다.
2. **apply** — 그 감사 결과에서 투자 대비 효과(ROI)가 높은 개선을 실제로 적용합니다.
3. **무인 검증 루프**(loop-run · loop-review · loop-lessons · loop-build) — 코드 변경을 사람이 매 단계 지켜보지 않아도 자동으로 점검·수정·빌드아웃합니다.

`audit → apply → verify` 한 주기를 하나만 설치해서 씁니다.

---

## 한눈에

AI 에이전트(Claude 등)가 낯선 코드베이스에서 잘 일하려면, 사람에게 필요한 것과 같은 것이 필요합니다. 어디에 뭐가 있는지 알려주는 지도, 모듈별 맥락 문서, "이렇게 하지 마라"는 안티패턴, 의존 관계 그림, 검증 장치가 그것입니다. **audit** 은 이 다섯 가지가 얼마나 갖춰졌는지 점수로 측정하고, **apply** 는 부족한 것을 채워 넣습니다. 준비가 되면, **loop 계열** 스킬이 그 위에서 실제 코드 작업을 무인으로 돌립니다.

> **이 문서 읽는 법** — 플러그인이 처음이면 위에서부터 읽으세요. 특정 스킬만 궁금하면 [스킬 6개](#스킬-6개) 로 바로 가세요. 무인 루프(loop 계열)를 볼 거라면 먼저 [알아둘 용어](#먼저-알아둘-용어-loop-계열) 를 훑어두면 뒤가 쉽습니다.

---

## 설치

```
/plugin marketplace add kunsanglee/ai-ready
/plugin install ai-ready@ai-ready
```

## 업데이트

이미 설치했다면 아래를 **각각 한 줄씩** 입력합니다. 슬래시 명령은 한 줄에 하나만 받으므로 주석을 같은 줄에 붙이면 마켓플레이스 이름으로 오해석됩니다.

```
/plugin marketplace update ai-ready
```
```
/plugin update ai-ready@ai-ready
```
```
/plugin list
```

> Claude Code 는 `plugin.json` 의 `version` 필드가 바뀐 경우에만 새 버전으로 인지합니다. 이 저장소는 매 릴리스에 버전을 올립니다. 설치된 버전은 위 `/plugin list` 가, 최신 버전은 아래 [변경 이력](#변경-이력) 첫 줄이 알려줍니다. 이 문장에 번호를 박아 두면 릴리스마다 갱신을 빠뜨려 어긋납니다 — 실제로 v0.8.8 에서 멈춰 있었습니다.

## 요구사항

로컬 셸과 git 만 있으면 됩니다. 외부 서비스 인증은 필요 없습니다.

- Python **3.10 이상**(감사 스크립트가 `X | None` 유니온 타입 표기를 사용 — 3.9 이하는 import 시점에 문법 오류), `jq`, `bash` 3.2+ (macOS 기본 환경 호환), `git`
- 감사·문서 생성 스크립트는 **표준 라이브러리만** 씁니다(외부 의존성 0).

---

## 무엇을 담고 있나 — 두 축

플러그인은 성격이 다른 두 묶음으로 나뉩니다.

**축 A · 문서화와 AI 준비도** — 코드베이스를 에이전트가 읽기 좋게 만드는 쪽입니다. `audit` 이 진단하고 `apply` 가 처방합니다. 산출물은 사람과 에이전트가 함께 읽는 문서(CLAUDE.md, INDEX.md, ARCHITECTURE.md, ANTIPATTERNS.md 등)입니다.

**축 B · 무인 검증 루프** — 준비된 코드베이스 위에서 실제 코드 변경을 자동으로 검증·수정·빌드아웃하는 쪽입니다. `loop-run`(변경 하나 수렴), `loop-review`(1회 점검), `loop-lessons`(실수 회수), `loop-build`(설계를 여러 단계로 빌드아웃)로 구성됩니다. 이 넷은 같은 판정 엔진(`_loop-engine`)을 공유합니다.

두 축은 한 지점에서 만납니다. audit 이 만드는 `docs/ANTIPATTERNS.md` 가 loop 계열의 **지식층**이 됩니다. 루프가 점검할 때 이 문서를 읽고, `loop-lessons` 가 새로 잡은 실수를 여기에 덧붙입니다.

---

## 먼저 알아둘 용어 (loop 계열)

무인 루프 설명에 반복해 나오는 말들입니다. 여기만 훑어두면 뒤가 술술 읽힙니다.

| 용어 | 뜻 |
|---|---|
| **maker** | 실제로 코드를 작성·수정하는 주체. 전용 `loop-maker` 서브에이전트가 맡습니다(기본 모델 opus·effort high, 모델은 호출 시 조정 가능). loop-run 은 회차마다, loop-build 는 단계마다 새로 띄웁니다. |
| **checker** | maker 가 고친 코드를 독립된 시각으로 점검하는 주체. 결함의 종류와 위치만 태깅합니다. 매번 새로 띄웁니다. 모델은 세션을 상속하지만 effort 는 `xhigh` 로 고정합니다 — 만드는 쪽은 싸게, 판정하는 쪽은 비싸게가 이 루프의 전제입니다. |
| **오케스트레이터** | 직접 코딩하지 않고 순서를 관리하는 조율자. 당신이 명령을 친 메인 세션이 이 역할입니다. |
| **서브에이전트** | 메인 세션이 따로 띄우는 독립 작업 에이전트. 자기만의 대화 맥락을 가져서, 여기서 한 작업이 메인 맥락을 어지럽히지 않습니다. |
| **rubric(루브릭)** | "무엇을 통과로 볼지"와 "언제 멈출지(회차·시간·예산 상한)"를 적어둔 채점 규칙표. LLM 주관이 아니라 이 규칙대로 기계적으로 판정합니다. |
| **severity(심각도)** | checker 가 찾은 결함의 등급. 심한 순으로 `BLOCKER` · `CRITICAL` · `MAJOR` · `MINOR`. 통과 조건은 "BLOCKER 0개 그리고 CRITICAL 0개". |
| **게이트** | 통과해야 다음으로 넘어가는 관문. 시작 게이트(사람 승인), 빌드 게이트(컴파일·기존 테스트), 종료 게이트(severity 판정) 셋이 나옵니다. |
| **brake(브레이크)** | 무인 실행이 무한정 돌지 않게 강제로 멈추는 안전 상한. 회차·시간을 다 쓰면 걸려 사람을 부릅니다. |
| **phase / step** | phase 는 설계를 독립적으로 개발·검증할 수 있는 큰 덩어리, step 은 그 안의 세부 작업(loop-build 전용 개념). |
| **워크트리** | 이 작업만을 위한 격리된 작업 폴더(git worktree). 변경이 여기 쌓이고 커밋 전까지 메인 브랜치를 건드리지 않습니다. |

> **왜 고치는 쪽(maker)과 점검하는 쪽(checker)을 나누나?** 자기가 짠 코드를 자기가 검토하면 결함을 놓칩니다. 글쓴이가 자기 글의 오탈자를 잘 못 잡는 것과 같습니다. 그래서 쓴 사람과 교정 보는 사람을 나누고, 교정자는 매번 선입견 없는 새 눈으로 바꿉니다.

---

## 스킬 6개

### `audit` — AI 준비도 채점 + 개선 로드맵

코드베이스가 AI 에이전트에게 얼마나 친화적인지 7개 카테고리로 채점하고, 무엇부터 고칠지 우선순위를 매긴 리포트와 대시보드를 만듭니다.

- **트리거** — `/ai-ready:audit`, "AI 준비도", "코드베이스 감사", "score my codebase for AI agents", "모듈별 CLAUDE.md 생성"
- **입력** — 대상 코드베이스 경로. 선택으로 스캐폴드할 상위 모듈 수(기본 5개), 안티패턴을 캐낼 git 이력 기간(기본 180일).
- **산출물** — 대상의 `.ai-ready/` 아래에 생성합니다.
  - `audit.json` — 카테고리·규칙별 원시 점수와 근거
  - `audit-report.md` — ROI 우선순위 개선 액션 목록(사람이 읽는 로드맵)
  - `dashboard.html` — 게이지·카테고리 막대·점수 추이 스파크라인을 담은 자체완결 대시보드
  - `history/{시각}.json` — 실행 이력(추이 렌더용, 삭제 금지)
  - `scaffolds/` — 누락 문서 초안(모듈별 CLAUDE.md 등)과 git 이력에서 추출한 `ANTIPATTERNS.md` 시드
  - `hooks/freshness_check.sh` + `freshness_check.py` — 문서 신선도 점검 훅(자기완결 복사본)
  - `README.md` — 산출물 소비 가이드(무엇부터 읽고 어디에 옮기는지)
- **평가 카테고리 (7개, 총 100점)**

  | # | 카테고리 | 무엇을 보나 |
  |---|---|---|
  | 1 | 내비게이션 | 루트에서 모듈까지 찾아가는 경로·문서 참조 |
  | 2 | 컨텍스트 문서 품질 | CLAUDE.md 와 문서의 내용 충실도 |
  | 3 | 암묵지 & 안티패턴 | ANTIPATTERNS.md, git 핫스팟에서 드러나는 함정 |
  | 4 | 모듈 간 의존성 추적 | ARCHITECTURE.md, 의존 그래프 |
  | 5 | 검증 게이트 | CI 훅, 테스트, 린트 같은 기계적 검증 장치 |
  | 6 | 신선도 자동 유지 | 문서가 코드와 함께 갱신되게 하는 장치 |
  | 7 | 성과 지표 | 결과 측정·추적 인프라 |

- **점수 등급**

  | 등급 | 점수 | 상태 |
  |---|---|---|
  | AI-blind | 0–39 | 에이전트가 컨벤션·경계 없이 헤맴. 매 PR마다 같은 맥락을 다시 설명 |
  | AI-aware | 40–59 | 일부 가이드 존재. 핫 모듈에만 도움 |
  | AI-enabled | 60–79 | 모듈 가이드·안티패턴·의존 그래프를 갖춰 에이전트가 자율 작업 가능 |
  | AI-maximalist | 80–89 | 신선도 자동 갱신 + 측정 인프라 운영 |
  | Agentic-ready | 90–100 | 다중 에이전트가 자율적으로 PR 을 만들 수 있는 상태 |

- 멀티모듈과 단일모듈(패키지=논리 모듈) 레이아웃을 자동 감지해 채점 규칙과 스캐폴드 출력을 달리합니다.
- 대상에 `.ai-ready/config.json` 을 두면(선택) frontmatter 인식 INDEX 그룹화·lazy-load 트리거 보존·프로젝트 현실을 반영한 채점(통합 design 디렉터리를 ADR 로 인정 등)을 활성화합니다. 없으면 기본 동작 그대로입니다.

### `apply` — 감사 결과를 실제로 적용

`audit.json` 과 `audit-report.md` 를 읽어, ROI 상위 개선 액션을 차례로 실행합니다.

- **트리거** — `/ai-ready:apply`, "audit 적용", "ROI 액션 실행", "우선순위 액션 적용"
- **입력** — 대상 디렉터리(이미 `audit.json` 이 있어야 함).
- **처리 방식** — 각 액션을 성격에 따라 넷 중 하나로 다룹니다.
  - **maintain(외과적 유지보수)** — 스크립트가 사실만 모으고(`--json` 읽기 전용), AI 가 현재 문서를 읽어 **새 항목만 추가하고 바뀐 것만 고칩니다.** 사람이 손댄 그룹·순서·산문은 보존합니다. 통째 덮어쓰기를 하지 않습니다.
  - **judgment** — AI 가 초안을 쓰고 사용자가 승인합니다(안티패턴 항목, ADR, "DO NOT" 섹션 등).
  - **mechanical** — 멱등 스크립트를 실행합니다(모듈 CLAUDE.md 스캐폴드, ARCHITECTURE.md 다이어그램, 신선도 훅 설치 등).
  - **skip** — 이미 충족된 항목.
- **사람이 인수한 문서 보호** — 생성 대상 파일에 자동 생성 서명이 없으면(사람이 직접 다듬은 문서라는 뜻) 덮어쓰기를 거부하고 멈춥니다. `--force` 로만 우회하며, 적용은 문서별 diff 확인 루프를 거쳐 승인분만 반영합니다.

### `loop-run` — 변경 하나를 통과까지 무인 수렴

사람이 작업을 맡기고 빠지면, `maker`(고치기) → `checker`(독립 점검) → 결정론 채점 → 정체 판정을 통과 기준을 만족할 때까지 반복합니다. 코드를 실제로 고치며 N회 수렴합니다.

- **트리거** — `/loop-run [회차]`, "루프 돌려", "이 작업 루프로 수렴시켜"
- **입력** — 작업 지시 **파일**(무엇을 만들/고칠지 + 완료 기준), 비교 베이스(기본 `origin/main`), 회차 상한(선택, 기본 5회·하드 천장 10회). maker 가 서브에이전트라 이 세션의 대화 맥락을 물려받지 못하므로 지시는 파일이어야 합니다. 합의가 대화에만 있으면 오케스트레이터가 브리프 한 장을 써서 사람에게 확인받은 뒤 시작합니다 — 무인 실행에서 사람이 거치는 유일한 관문입니다.
- 그 확인과 **같은 화면**에서 `loop-spec-checker` 가 그 지시가 답하지 않은 결정을 열거합니다(v0.9.12). 사람이 아직 있는 마지막 순간이라 여기서 답하면 되고, **시작을 막지는 않습니다** — 무엇이 결정적인지는 프로젝트마다 다르고, 기계가 막으면 사람이 우회법부터 배웁니다.
- **산출물** — 수렴된 변경(워크트리에 쌓이고 커밋은 하지 않음) + 사이클 로그(`.loop/run/{ticket}/history.jsonl`).
- 빌드·테스트·린트 명령, 티켓 패턴, 베이스 브랜치는 `detect_build.py` 가 런타임에 감지해 어댑터 파일을 남기지 않습니다.
- 게이트가 깨지면 그 출력은 창이 아니라 파일로 가고, `gate_parse.py` 가 항목 큐(`.loop/run/{ticket}/gate-queue.jsonl`)로 바꿉니다. maker 는 컴파일 오류 → 테스트 실패 → 린트 위반 순으로 항목을 꺼내 고칩니다.
- maker 는 회차마다 새로 띄우는 서브에이전트이고 **오케스트레이터 세션은 코드를 쓰지 않습니다**. 회차를 건너 전달되는 것은 대화가 아니라 워킹 트리와 반복 표시(`history.jsonl` 에서 뽑는 finding 단위 반복 횟수)입니다. 게이트를 돌리기 전에 트리가 실제로 바뀠는지 확인합니다 — 보고는 틀릴 수 있어도 트리는 그럴 수 없습니다.

### `loop-review` — 1회 점검·보고 (코드는 안 고침)

무인 루프와 같은 판정 엔진으로 현재 변경을 **한 번만** 점검해 등급순 리포트를 냅니다. 고치는 건 사람이 합니다.

- **트리거** — `/loop-review [--html]`, "loop 리뷰", "이 변경 점검"
- **입력** — 현재 브랜치 변경(기본 `origin/main..HEAD` + 커밋 전 변경), 작업 정의(선택).
- **산출물** — 마크다운 리포트(기본) 또는 `--html` 로 자체완결 HTML. 등급별(BLOCKER→CRITICAL→MAJOR→MINOR) finding 과 판정 요약.
- 빌드·테스트를 돌리지 않고 점검만 하므로 빠릅니다.

### `loop-lessons` — 잡은 실수를 재발 방지 지식으로

루프가 끝난 뒤, 잡힌 실수와 사람·PR 지적을 모아 `docs/ANTIPATTERNS.md` 후보 초안을 만들고, **사람이 하나씩 승인**해서 반영합니다.

- **트리거** — `/loop-lessons [--history <경로>]`, "lesson 종합", "안티패턴 후보"
- **입력** — 루프 이력(`history.jsonl`, 자동), 선택으로 사람·PR 지적, 티켓 요약.
- **산출물** — `docs/ANTIPATTERNS.md` 에 승인 항목 추가(통째 덮어쓰기 없음), 필요 시 프로젝트 LOCAL 루브릭에 예외 한 줄 추가.
- **수록 문턱** — 같은 자리 수정이 3회 이상 반복되거나 revert 가 난 것만 올립니다. 일회성 국소 실수는 버립니다. 무인 루프여도 이 반영 단계만은 반드시 사람 승인을 거칩니다.

### `loop-build` — 설계를 여러 단계로 무인 빌드아웃

확정된 설계 문서를 phase/step 으로 쪼개 사람이 승인한 뒤, 각 phase 를 maker 서브에이전트로 개발하고 loop-run 의 판정 엔진으로 통과까지 수렴시키며 여러 phase 를 순차 전진합니다. **loop-run 을 감싼 멀티-phase 확장**입니다.

- **트리거** — `/loop-build [phase당 회차] [설계 문서 경로]`, "설계대로 쭉 빌드해", "phase 순회 루프"
- **입력** — 설계 문서/spec 경로(형식 무관, 오케스트레이터가 phase 로 분해), phase 당 회차(선택, 기본 5). 인자는 타입으로 구분해 순서가 바뀌어도 안전합니다.
- **동작** — Step 0 셋업 → Step 1 분해 → **착수 전 스펙 검사**(기계) → **스펙 완전성 점검**(`loop-spec-checker`) → **사람 승인(유일한 시작 게이트)** → Step 1-1 순회를 서브에이전트에 위임 → Step 2 phase 순회(각 phase 마다 새 maker, 빌드 게이트 → checker → 채점 → 분기) → Step 3 완료 보고. 커밋은 하지 않고 워크트리에 누적합니다.
- **핵심 성질**
  - 순회는 **언제나 서브에이전트에 내립니다**(v0.9.12). 사이클 잡음(채점 결과·maker 지시·게이트 출력)이 사람이 보는 창에 안 쌓이고, 메인에는 phase 가 하나 닫힐 때마다 한 줄이 올라옵니다.
  - 착수 전 스펙 검사 셋(`exit_criteria`·`irreversible`·`tiebreaks`)을 **통과해야 시작합니다**. 못 통과하면 다른 방식으로 도는 것이 아니라 분해를 고쳐 다시 옵니다 — 검사 옆에 우회로가 있으면 그 검사는 권고가 됩니다.
  - 회차·시간·예산 상한을 **phase 하나 기준**으로 봅니다(loop-run 은 작업 전체 기준). 전체 시간 상한은 `phase당 120분 × phase수` 로 자동 확장돼, phase 가 늘어도 뒤 phase 가 시간에 잘리지 않습니다.
  - maker 는 phase 마다 새 서브에이전트로 격리하되, **한 phase 안의 재시도는 같은 maker 를 이어 부릅니다**(수정 맥락 유지).
  - 무인 순회를 시작·재개하기 직전 분해 결과(`phases.json`)가 온전한지 검증하고, 손상됐으면 즉시 멈춰 사람을 부릅니다(v0.8.1).
  - 구현이 설계와 어긋나야 한다고 판단되면 임의로 설계를 바꾸지 않고 멈춰 사람에게 재결정을 맡깁니다. 목표는 "설계대로 구현"이지 "설계를 고쳐 구현"이 아닙니다.

### 언제 무엇을 쓰나

| 상황 | 스킬 |
|---|---|
| 코드베이스가 AI 에게 얼마나 준비됐는지 점수·로드맵을 보고 싶다 | `audit` |
| 그 로드맵의 개선을 실제로 적용하고 싶다 | `apply` |
| 변경 **하나**를 통과까지 무인으로 수렴시키고 싶다 | `loop-run` |
| 지금 변경을 고치지 않고 **한 번** 점검만 받고 싶다 | `loop-review` |
| 루프가 잡은 실수를 재발 방지 지식으로 정리하고 싶다 | `loop-lessons` |
| **설계 전체**를 여러 단계로 무인 빌드아웃하고 싶다 | `loop-build` |

---

## 판정 엔진은 어떻게 판단하나 (`_loop-engine`)

loop 계열 넷은 같은 결정론 판정 엔진을 공유합니다. 핵심은 **"등급을 LLM 이 매기지 않는다"** 입니다.

**checker(LLM)는 결함의 종류와 위치만 찾고, 등급은 규칙표를 보고 셸이 매깁니다.** LLM 이 등급을 매기면 같은 코드에도 실행마다 판정이 흔들려 통과 여부가 불안정해집니다. 종류·위치만 LLM 이 태깅하고 심각도는 결정론 셸이 매기면, 같은 코드엔 항상 같은 등급이 나옵니다.

- **checker 의 5개 점검 차원** — `compatibility`(하위 호환), `security`(권한·입력 검증), `runtime`(동시성·트랜잭션·N+1·멱등성 등), `intent`(작업 정의 ↔ 코드 정합), `convention`(네이밍·컨벤션·테스트 누락).
- **severity 4단계와 행동**

  | 등급 | 행동 | 뜻 |
  |---|---|---|
  | BLOCKER | 사람 대기(AWAIT_USER) | maker 가 못 고치거나 고치면 안 됨. 비가역·자동화 금지 |
  | CRITICAL | 재시도(RETRY) | 진짜 결함이나 maker 가 고칠 수 있음 |
  | MAJOR | 계속 개선(RETRY_SOFT) | 고치면 좋으나 통과 가능. 정체 시 사람 승인으로 통과 |
  | MINOR | 통과(기록만) | 사소 |

- **통과는 점수 합산이 아니라 게이트** — 사소한 결함이 10개라도 심각한 게 없으면 통과, 단 하나라도 BLOCKER 면 불통과입니다. "총점 평균"이 아니라 "가장 심각한 게 무엇이냐"로 문을 엽니다.
- **비가역 영역은 등급과 무관하게 사람 대기** — 운영 DB 변경, 돈·정산, 인가 정책, 대량 발송, 삭제·탈퇴는 점수가 만점이어도 멈춰 사람을 부릅니다.
- **상한 기본값(단일 원천)** — 회차 5(천장 10), 시간 120분, 비용 $500, 토큰 5M. 정체·악화 감지 파라미터도 같은 표에 있습니다.
- **BASE + LOCAL 루브릭** — 프로젝트 무관 골격은 `_loop-engine/rubric.base.md`(BASE)에, 프로젝트 특유 규칙은 대상의 `.loop/rubric.md`(LOCAL)에 둡니다. 둘은 병합되며 같은 항목은 LOCAL 이 우선합니다.

엔진을 이루는 조각은 이렇습니다.

| 파일 | 책임 |
|---|---|
| `score.sh` | checker 가 낸 결함마다 규칙표로 severity 를 매김 |
| `decide.sh` | 매겨진 결함을 모아 판정(PASS/RETRY/AWAIT_USER 등)을 냄 |
| `stall.sh` | 회차 간 정체·악화를 감지 |
| `lessons.sh` | 루프 이력에서 "고쳐진 실수"를 추출(loop-lessons 입력) |
| `detect_build.py` | 매니페스트·브랜치를 읽어 빌드·테스트·린트 명령, 티켓 패턴, 베이스 브랜치, 컨벤션 문서를 런타임 감지 |
| `gate_parse.py` | 게이트(빌드·테스트·린트) 실패 출력을 한 줄 하나의 JSON 항목으로 바꿈. maker 가 항목 단위로 꺼내 고친다 |
| `rubric.base.md` | BASE 루브릭(사람이 읽고 편집하는 규칙 데이터) |
| `test.sh` | 채점 로직의 결정론 회귀 테스트 |

세 에이전트가 루프의 각 시점을 맡습니다. **`loop-spec-checker`** 는 루프가 **돌기 전에** 그 지시가 답하지 않은 결정을 네 렌즈로 열거합니다. **`loop-checker`** 는 **도는 동안** 위 5개 차원으로 변경을 적대적으로 점검해 구조화 결과를 냅니다. **`loop-lesson-synthesizer`** 는 **끝난 뒤** 실수를 안티패턴 후보로 정리합니다. 셋 다 **코드를 수정하지 않습니다**(읽기 전용) — 그 계약은 산문이 아니라 각 정의의 `tools` 줄이 강제하고, 시험이 그 줄을 셉니다.

---

## 설계 원칙

- **심각도는 결정론 셸이 매긴다** — 같은 코드엔 같은 등급. 판정의 재현성을 보장합니다.
- **문서는 AI 가 외과적으로 유지보수한다** — 사실은 스크립트가 모으고, 새 항목 추가와 변경분 수정만 AI 가 하며, 사람이 손댄 큐레이션은 보존합니다.
- **maker 와 checker 를 분리한다** — checker 는 코드를 수정하지 않고 maker 의 변명을 듣지 않습니다. 독립성이 신뢰의 근거입니다.
- **fail-loud** — 비었거나 형식이 어긋난 입력을 조용히 통과시키지 않고 즉시 멈춥니다. loop-build 는 무인 순회 직전 `phases.json` 을 이렇게 검증합니다.
- **사람 승인을 의무화한다** — apply 의 매 액션, loop-lessons 의 모든 반영, loop-build 의 분해 승인이 사람 게이트입니다.
- **비가역 영역은 사람이 판단한다** — 운영 DB·돈·인가·대량 발송·삭제는 자동화하지 않습니다.

---

## 저장소 구조

```
.
├── .claude-plugin/marketplace.json   # 마켓플레이스 manifest
└── plugins/
    └── ai-ready/
        ├── .claude-plugin/plugin.json
        ├── skills/
        │   ├── audit/         # AI 준비도 채점 + 리포트·대시보드·시드 생성
        │   ├── apply/         # ROI 액션 적용 (외과적 문서 유지보수)
        │   ├── loop-run/      # 변경 하나를 통과까지 무인 수렴
        │   ├── loop-review/   # 1회 점검·보고
        │   ├── loop-lessons/  # 잡은 실수 → 지식층 반영 (사람 승인)
        │   └── loop-build/    # 설계를 여러 phase 로 무인 빌드아웃
        ├── agents/            # loop-maker · loop-spec-checker · loop-checker · loop-lesson-synthesizer
        ├── _loop-engine/      # 결정론 채점 셸 + BASE 루브릭 + detect_build.py + gate_parse.py
        └── tests/             # 회귀 시험 (stdlib unittest — audit 스크립트 · 스킬 문서 셸 블록 · 에이전트 정의)
```

시험은 세 층입니다. `_loop-engine/test.sh` 가 채점 결정론을, `tests/test_agent_defs.py` 가 에이전트 정의를(읽기 전용 계약이 `tools` 줄에서 새지 않는지, 그리고 그 정의가 매니페스트에 등록돼 실제로 로드되는지), `tests/test_skill_blocks.py` 가 스킬 문서 안 셸 블록을 지킵니다. 뒤쪽은 SKILL.md 에서 블록을 뽑아 격리 레포에서 **프레시 셸마다** 실제로 돌립니다. 오케스트레이터가 그 블록을 그대로 Bash 도구에 넣어 실행하므로 블록이 곧 코드이고, 문법 검사로는 프레시 셸에서 변수가 복원되는지·fail-loud 경로가 실제로 멈추는지·지운다고 말한 것이 지워지는지를 알 수 없습니다. `tests/mutate_skill_blocks.py` 는 그 시험에 이가 있는지를 봅니다 — 지난 릴리스에서 실제로 있었던 결함을 문서 사본에 되넣고 시험이 깨지는지 확인합니다. 초록 50건은 "결함이 없다" 와 "결함을 볼 눈이 없다" 를 구별하지 못하기 때문입니다.

---

## 변경 이력

- **0.9.12** — `loop-build` 의 **직접 모드를 없앴습니다.** 순회는 언제나 서브에이전트에 내립니다. 0.9.11 의 착수 전 스펙 검사가 실패하면 직접 모드로 도는 길이 옆에 있었는데, 검사 옆에 우회로가 있으면 그것은 검사가 아니라 권고였습니다. **`exit_criteria`·`irreversible`·`tiebreaks` 세 자리가 필수가 됩니다.** 없으면 시작하지 않고 무엇이 빠졌는지 이름으로 알립니다. 유예 기간은 없습니다. `irreversible` 은 `false` 또는 영역 이름 문자열만 받고 `true` 는 거부합니다(닿는데 어딘지 안 적은 값이라 사람에게 올릴 이름이 없습니다). 새 에이전트 **`loop-spec-checker`** 가 `/loop-run`·`/loop-build` 입구에서 그 지시가 답하지 않은 결정을 열거합니다. 경고까지고 시작을 막지 않습니다. **업그레이드 시 확인할 것 하나** — 진행 중이던 `loop-build` 가 있으면 재개 전에 그 `phases.json` 에 세 자리를 채우십시오. 0.9.11 에서 만든 분해는 이 판정을 통과한 적이 없어 재개가 멈춥니다. 시험 142 → 146.

- **0.9.11** — 같은 날 같은 저장소에서 `loop-build` 로 두 phase 를 돌렸는데 **한쪽은 여섯 사이클에 사람이 한 번 끼어들어야 닫혔고, 다른 쪽은 네 사이클에 사람이 한 번도 필요 없었습니다.** 차이는 스펙 하나뿐이었습니다 — 앞쪽은 목표가 "세웠다고 적은 장치가 실제로 잠기게 한다" 라 항목으로 나열되지 않아 하나를 닫으면 checker 가 다음을 찾았고, 뒤쪽은 목표를 **변이 여섯으로 미리 적어** 줬습니다("관성 분기를 지우면 빨개진다" 같은 것). 그래서 **사람이 끼어들어야 하는 자리는 대부분 미리 알 수 있다** 는 것을 규약으로 옮깁니다. `loop-build` 에 **위임 모드** 를 선택지로 더했습니다 — 순회를 통째로 서브에이전트에 내려 사이클 잡음(채점 결과·maker 지시·게이트 출력)이 사람이 보는 창에 안 쌓이게 하는 모드입니다. 아래층 출력은 띄운 쪽에게 가므로 그 잡음이 위로 안 올라옵니다(실측: 3,900자 출력을 그 깊이에서 냈을 때 중간 에이전트만 전문을 받았고 최상위에는 네 줄 요약만 왔습니다). **종전 방식은 그대로 둡니다** — 사람이 자주 끼어들 일이면 직접 모드가 맞고, 매번 고릅니다. 위임의 관문은 실행 중 판정이 아니라 **착수 전 스펙 검사 셋** 입니다: phase 마다 `exit_criteria`(각 항목이 "되돌리면 무엇이 빨개지나" 를 말한다), phase 마다 `irreversible`(닿는 영역을 문자열로, 안 닿으면 `false`), 최상위 `tiebreaks`(측정으로 안 갈리는 트레이드오프의 순서). 셋 중 하나라도 없으면 `exit 65` 로 멈추고 위임하지 않습니다. 이 관문은 Step 1 의 사람 승인과 **별개의 기계적 검사** 이고, 무엇이 빠졌는지 이름으로 지목합니다 — `jq -e` 한 방이면 "뭔가 빠졌다" 까지만 나와 사람이 무엇을 더 적을지 못 읽습니다. **기존 `phases.json` 은 그대로 돕니다** — 이 셋은 위임 검사에서만 요구하고 Step 2 의 순회 입력 검증은 손대지 않았습니다(호환성 대조군을 시험에 함께 뒀습니다. 둘 다 거부하면 기존 사용자가 그날로 멈춥니다). 위임 오케스트레이터가 **위로 올리는 것은 셋뿐** 입니다: 비가역 영역, 범위가 바뀌는 것(두 phase 실측에서 사람에게 올라간 유일한 종류), 그리고 설계가 미완이 아니라 **틀렸을** 때. 근거가 측정이면 올리지 않습니다 — 재서 갈리는 것은 그 층이 재고 정하고, `tiebreaks` 에 답이 있으면 그것도 안 올립니다. **그 층은 사람에게 직접 말할 수 없어** 자기 턴을 끝내고 메인에 보고하면 메인이 전달하고 답을 `SendMessage` 로 되돌립니다. 아래층 스폰에는 제약이 둘 있습니다 — 그 깊이에서는 `run_in_background` 와 `name` 을 못 써서 maker·checker 를 동기로 띄우고 `agentId` 로 잇고(한 phase 안의 RETRY 를 같은 maker 로 잇는 불변은 그대로), 그 층은 팀 명부에도 화면에도 안 보여 진행은 `.loop/run/` 의 이력 파일로 봅니다. 단언 57 → 62(스킬 블록).

- **0.9.10** — `loop-build` 로 phase 하나를 돌렸는데 **여섯 사이클이 걸렸고 그중 넷이 같은 종류**(`test-vacuous`)였습니다. 1회차는 "고친 코드가 안 잠겼다", 2회차는 "잠그려고 세운 검사가 안 잠겼다", 3회차는 "그 검사가 잠그는 것은 상수의 내용뿐이다" — 매번 한 겹씩 깊어지고 등급은 나빠졌습니다. **원인은 코드가 아니라 phase 목표였습니다.** 목표가 "세웠다고 적은 장치가 실제로 잠기게 한다" 였는데 이건 끝나는 지점이 없는 목표입니다. 하나를 잠그면 checker 가 다음 안 잠긴 것을 찾고, 사람이 범위를 하나로 좁혀 준 뒤에야 닫혔습니다. **판정부는 그동안 아무 신호도 내지 않았습니다** — `stall.sh` 는 등급 개수 벡터만 보고 finding 의 종류는 안 봐서, 같은 종류가 계속 나온다는 사실이 어디에도 안 잡히고 등급이 오르내리는 동안 `PROGRESS` 까지 떴습니다. 이제 `kindstreak.sh` 가 사이클마다 그 사이클을 지배한 종류(가장 높은 등급의 finding 들 중 최빈 kind)를 하나 정하고, 마지막 사이클부터 거슬러 같은 종류가 연속 지배한 횟수를 셉니다. `repeated_kind_cycles`(기본 3, rubric PARAMS) 이상이면 멈추고 사람을 부르되 **전할 말이 정체와 다릅니다** — "코드가 안 고쳐진다" 가 아니라 "같은 종류가 N 사이클 지배했다, 코드가 아니라 목표를 의심하라" 이고, 목표가 열거 가능한지와 끝나는 지점이 정의됐는지를 사람이 봅니다. MINOR 만 있는 사이클은 게이트를 통과시키는 등급이라 연속을 끊지도 잇지도 않고 건너뛰며, 최빈이 동점이거나 finding 이 없는 사이클은 연속을 끊습니다. 이력을 못 읽으면 `exit 65` 로 시끄럽게 죽습니다 — 조용히 `OK` 를 내면 감지기가 영영 안 울리는데 그 침묵이 통과로 보입니다. **finding 의 `severity` 가 사다리 밖이거나 없을 때도 같습니다.** 처음 판은 그런 finding 을 MINOR 와 같은 길로 보내 건너뛰었고, 등급 오타 하나(`CRITCAL`)에 같은 종류가 네 사이클 연속인 이력이 `streak: 0` 으로 나왔습니다 — 감지기가 눈이 먼 채 통과를 냈습니다. 이 감지기를 만들게 한 병(확인 못 한 것이 통과 방향으로 떨어지는 것)과 같은 종류라, `MINOR` 건너뛰기는 규칙으로 두고 사다리 밖 값은 어느 사이클의 어떤 값인지 짚어 거부합니다. `stall.sh` 는 건드리지 않았습니다(입력 계약을 바꾸는 대신 옆에 세웠습니다). `loop-build` 의 "좋은 step 의 원칙" 에는 **사전** 규율을 적었습니다 — step 마다 실행 가능한 AC 가 있어도 phase 목표가 열거 불가능하면 수렴하지 않으므로, 분해 단계에서 "무엇을 다 하면 이 phase 가 끝인가" 를 한 줄로 적을 수 있는지 확인합니다. 감지기는 그것이 새어 나갔을 때의 그물입니다. 단언 112 → 150(엔진).

- **0.9.9** — loop 계열 스킬의 셸 블록이 **없는 변수로 엔진 경로를 잡던 것**을 고칩니다. `$CLAUDE_PLUGIN_ROOT` 는 스킬 본문을 만들 때 치환되는 값이라 Bash 도구가 띄우는 자식 셸에는 존재하지 않습니다(실측: 빈 문자열). 그래서 `ENG="$CLAUDE_PLUGIN_ROOT/_loop-engine"` 은 `/_loop-engine` 이 됐고, **없는 경로가 있어 보이는 경로로 만들어지는** 종류라 뒤따르는 `loop_param` 이 빈 값을 내며 조용히 흘러갔습니다 — 루프를 걸 때마다 사람이 손으로 경로를 유도해야 했습니다. 이제 블록은 호스트가 스킬 본문 첫머리에 주입하는 "Base directory for this skill" 값을 자리표시자로 받아 엔진을 유도하고, 못 찾으면 그 자리에서 exit 65 로 죽습니다(`/loop-run`·`/loop-review`·`/loop-lessons`, 그리고 `audit`·`apply` 의 파이썬 스크립트 경로도 같은 방식). **이 결함이 오래 산 이유는 시험에 있었습니다** — 블록 시험 하네스가 `CLAUDE_PLUGIN_ROOT` 를 직접 주입해, 실제 환경에서는 안 도는 블록이 시험에서만 초록이었습니다. 그 주입을 걷어내고 대조군 셋을 더했습니다: 자리표시자를 안 붙여 넣으면 비0 으로 죽는가, 붙여 넣으면 실제로 엔진을 찾는가, 옛 표기는 이 셸에서 아무것도 못 찾는가. 마지막 하나가 주입 부활을 잠급니다. 블록 전수 검사도 더해 어느 스킬이든 그 변수를 다시 쓰면 시험이 먼저 멈춥니다.
- **0.9.8** — 플러그인 목록 화면에서 읽는 설명문을 **19,805자에서 754자로** 줄입니다. 릴리스마다 문단을 하나씩 덧붙여 온 결과 매니페스트 설명문이 변경 이력 전문이 됐고, 목록이 읽을 수 없는 상태였습니다. 이력은 전부 이 README 변경 이력에 있으므로 잃는 내용 없이 옮깁니다(설명문에 있던 v0.2.0~v0.9.7 은 이 목록의 부분집합입니다). 대신 설명문은 이 플러그인이 무엇인지를 정확히 말합니다 — 종전 첫 문단이 감사·적용 두 스킬만 소개하고 0.6.0 부터 절반을 차지한 loop 계열을 통째로 빠뜨리고 있었습니다. 그리고 **0.9.7 이 붙인 "설명문이 이 릴리스를 언급하나" 검사를 길이 상한(1,200자)으로 바꿉니다** — 그 검사를 통과하는 가장 쉬운 길이 문단을 하나 더 붙이는 것이라, 부풀림을 막겠다고 붙인 검사가 부풀림의 유인이었습니다. 릴리스 이력의 단일 출처는 README 이고 그쪽은 별도 검사가 지킵니다.

- **0.9.7** — 채점에서 **모델이 조용히 등급을 정하던 자리**를 표로 옮기고, "안 본 것" 을 통과로 안 읽습니다. `severity 는 checker 가 매기지 않는다` 는 참이지만 등급을 정하는 입력 셋(`kind`·`dimension`·`weights`)이 전부 checker 에서 왔습니다 — 같은 운영 DDL 삭제가 가중 표시 하나로 `RETRY` 와 `AWAIT_USER` 로 갈렸고, 사람을 부를지를 단어 하나가 정하고 있었습니다. **자동화 금지 다섯에 `kind_id` 를 줬습니다**(`ddl-safety`·`money-path-change`·`authz-policy-change`·`mass-dispatch`·`destructive-data-op`) — 산문은 "표의 `force_await=always` 열로 적용한다" 고 적었는데 그 값을 쓰는 행이 **하나도 없어**, 사람 대기가 전적으로 프롬프트 준수에 걸려 있었습니다. 이제 종류 이름만 맞으면 표가 사람을 부릅니다. **`PATHWEIGHTS` 표**가 finding 의 경로에서 가중을 유도해 checker 것과 합치고(BASE 는 DB 마이그레이션 디렉터리만 — 돈·인가 경로는 저장소마다 달라 LOCAL 몫), **`PATHEXCLUDE` 표**가 문서·픽스처·vendor 를 빼 마이그레이션 정책 문서 오타 하나가 무인 루프를 세우던 것을 막습니다. 그 표는 LOCAL 이 BASE 경로 규칙을 끄는 유일한 길이기도 합니다. `test-vacuous` 를 종류로 세웠습니다 — `test-missing` 은 테스트가 *없는* 것만 잡고, 있는데 되돌려도 초록인 것은 이름이 없어 최하 등급으로 통과했습니다.

  **계약이 바뀝니다.** checker 는 이제 `{base, reviewed, findings}` 를 냅니다. `findings` 와 `reviewed` 가 둘 다 비면 채점이 `exit 65` 로 거부합니다 — `{"findings":[]}` 는 정상 형식이라 파일 가드를 지나서 "깨끗함" 과 "베이스 브랜치가 어긋나 diff 가 통째로 빈 것" 이 구분되지 않았고, 후자는 점검 없이 통과였습니다. 옛 checker 가 깨끗한 결과를 내면 멈추는데, 조용한 통과가 아니라 **시끄러운 사람 호출**이고 checker 프롬프트가 같은 버전에 함께 나갑니다.

  **적대적 검토 넷이 이 릴리스의 절반을 만들었습니다.** 가장 나쁜 것은 새 방어가 스스로 fail-open 이던 것입니다 — `reviewed` 자리에 배열이 아닌 값이 오면 길이 계산이 jq 에서 죽고 그 실패가 빈 문자열이 되어 조건이 거짓이 되며 게이트가 통째로 열렸습니다. 그 밖에 `force_await` 를 엄격 비교해 표의 어휘인 `"always"` 를 무시하던 것(부정 목록으로 뒤집음), 관대한 kind 가 dimension floor 를 완전히 이기던 것(어긋나면 높은 쪽), 배열 아닌 `weights` 가 같은 배치의 진짜 BLOCKER 까지 날리던 것, BASE 경로 패턴이 루트 `migrations/`·Rails `db/migrate/`·Liquibase `db/changelog`·대문자 경로를 놓치던 것(줄 번호를 떼고 대소문자 무시로 맞춤), LOCAL 한 줄로 금지행 다섯이 꺼지던 것(`force_await=always` 만 합집합 병합 — 그 파일은 채점받는 maker 가 쓸 수 있는 자리입니다), 열이 모자란 KINDS 행과 허용 표 밖·빈 `PATHWEIGHTS` 키가 조용히 사라지던 것(전부 `exit 65`), `stall.sh` 상태 파일에 검증이 없어 음수 하나로 정체 감지가 영구히 죽던 것, 그리고 **오케스트레이터가 `score.sh` 의 `exit 65` 를 삼켜** verdict 가 미정의로 흐르고 회차 카운터까지 멈추던 것을 고쳤습니다. 소비자 없는 `layer` 출력과 BASE 와 중복이던 `detect_build` 의 postgres 후보는 뺐습니다. 단언 33 → 112(엔진), 50 → 52(스킬 블록 하네스).

- **0.9.6** — Claude 트리에 effort 사다리를 적용해 판정부를 세션에서 떼어냅니다. (이 항목은 0.9.7 에서 소급 기록했습니다 — 0.9.6 릴리스가 이 목록과 매니페스트 버전 셋 중 둘을 함께 올리지 않아 `build/drift-test.sh` 에 그 누락을 잡는 검사를 붙였습니다.)

- **0.9.5** — `loop-run` 의 maker 를 세션에서 떼어내 loop 계열 넷의 계약을 맞춥니다. `loop-run`·`loop-build` 를 claude·codex 두 트리로 세면 넷인데 maker 를 메인 세션이 겸하는 것은 claude `loop-run` 하나뿐이었고, 그 차이가 어느 파일에도 적혀 있지 않아 의도인지 갈라진 것인지 판정할 근거가 없었습니다. 이제 넷 다 maker 를 서브에이전트에 위임하고 남은 차이는 수명 하나입니다 — `loop-run` 은 회차마다 새로, `loop-build` 는 phase 마다 새로 띄우고 그 phase 안의 재시도만 같은 maker 를 이어갑니다. 분리의 부수 이득으로 `loop-run` 도 모델 비대칭을 쓸 수 있게 됐습니다(구현은 `loop-maker` 기본값 opus, 검증은 세션 모델을 상속하는 checker). **작업 지시는 파일이어야 합니다** — maker 가 서브에이전트라 대화 맥락을 물려받지 못하고, 회차마다 요약을 다시 만들면 5회차 지시가 1회차와 달라집니다. maker 와 checker 가 같은 파일을 받으므로 둘이 서로 다른 요약으로 일하다 정합 판정이 어긋나는 일도 없어집니다. 합의가 대화에만 있으면 오케스트레이터가 브리프 한 장을 `$LOOP_DIR/brief.md` 에 쓰고 **사람에게 확인받은 뒤** 첫 회차를 엽니다 — 무인 실행에서 사람이 거치는 유일한 관문이고, 열 회차를 돌릴 지시를 사람이 한 번도 보지 않고 떠나는 것이 원래 위태로운 자리였습니다. 회차를 건너 전달되는 것은 대화가 아닙니다. 앞 회차가 무엇을 시도했는지는 워킹 트리에 그대로 있어 새 maker 가 `git diff` 로 읽고, 같은 결함이 몇 회차째인지는 `history.jsonl` 에서 뽑는 finding 단위 반복 표시가 알려줍니다. 같은 정보를 대화로 실어 나르는 쪽이 더 비쌉니다. **게이트를 돌리기 전에 트리가 실제로 바뀠는지 확인합니다** — maker 가 고쳤다고 보고하고 아무것도 안 바꿨으면 게이트가 같은 결과를, checker 가 같은 finding 을 내고 회차만 탑니다. 변경이 없으면 회차가 아니라 정체 신호입니다. "매 회차 코드가 바뀌어야 루프가 의미 있다" 는 요구에 처음으로 집행이 붙었습니다. maker 보고는 `ok` 또는 `blocked: <한 줄 사유>` 로 줄었습니다. 분기 입력이 셸이 낸 verdict·정체 판정·brake 카운터 셋뿐이라 산문 보고가 어느 분기에도 들어가지 않았고, `blocked` 만 `AWAIT_USER` 로 분기를 바꿉니다. `blocked` 를 다른 maker 로 돌리지 않고 `loop-build` 처럼 phase 요약이 필요한 자리에서만 오케스트레이터가 더 긴 보고를 명시로 요구합니다. `loop-maker` 는 이제 두 스킬이 공유하고, 빌드 명령을 받으면 컴파일만 스스로 돌려 확인합니다(테스트는 게이트의 몫). **성능 개선이 아니라 정합 맞추기입니다** — 오케스트레이터 컨텍스트 누적은 예측이고 측정치가 아닙니다. 남아 있는 루프 흔적은 전부 `loop-build` 이고 관측된 회차가 1~3이라, 이 릴리스는 없는 수치가 아니라 넷 사이의 불일치를 근거로 삼습니다.
- **0.9.4** — 게이트 실패 출력을 버리지 않고 항목 큐로 만듭니다. 종전에는 빌드·테스트가 깨지면 그 출력 전문이 오케스트레이터 창에 그대로 쏟아지고, 다음 회차에 또 쏟아졌습니다. checker 가 낸 결함은 `scored.json` 에 구조화돼 항목 단위로 처리되는데 게이트 실패만 원문 덩어리였습니다. 이제 `loop-run` 이 게이트 출력을 파일로 받고 새 `_loop-engine/gate_parse.py` 가 한 줄 하나의 JSON 항목(`kind`·`file`·`line_number`·`message`·`raw`)으로 바꿔 `gate-queue.jsonl` 에 쌓습니다. maker 는 컴파일 오류 → 테스트 실패 → 린트 위반 순으로 꺼내 고치고, 창에는 한 줄 목록만 나갑니다. 실측으로 빌드 출력 30줄이 항목 2건, 린트 46줄이 13건, 테스트 59줄이 1건이 됐습니다. **게이트 큐가 비어 있지 않으면 `scored.json` 보다 먼저입니다** — 게이트가 깨진 사이클은 checker 를 부르지 않아 그 파일이 앞 사이클 값으로 남아 있고, 그걸 먼저 고치면 이미 없는 문제를 쫓습니다. 큐는 사이클마다 비우고 새로 채웁니다. 게이트를 매 사이클 다시 돌리므로 그 출력이 유일한 정본이고, 앞 회차 항목이 남으면 고쳐진 오류를 계속 쫓게 됩니다. **아는 형식이 하나도 없어도 큐가 비지 않습니다** — 출력 꼬리를 `gate-output-unparsed` 항목 하나로 남깁니다. 조용히 버리면 큐가 비어 게이트가 통과한 것처럼 보이고 그 오독이 이 큐가 막는 실패입니다. 형식은 기억이 아니라 실제 출력에서 떴습니다. Kotlin 2.x 는 **열 번호 뒤에 콜론이 없어**(`...:17:31 Unresolved reference`) 기억대로 쓴 정규식은 한 줄도 못 맞혔을 것이고, 정규식과 테스트 픽스처를 같은 기억으로 쓰면 둘 다 초록인 채 실전에서 0건이 됩니다. 구버전의 콜론 형식도 함께 받습니다. 형식 회귀는 `test_gate_parse.py` 24건이 잡고 `test.sh` 에 붙어 있습니다.
- **0.9.3** — 금지 가이드 만점에 구체성을 요구합니다. 금지 줄을 줄 수로만 세면 "금지" 라는 단어를 세 번 적는 것이 최적 전략이 됩니다. 2회차 적대 검토가 실증한 키워드 농사입니다. 이제 만점은 백틱 코드 참조·경로·CamelCase 나 snake_case 식별자 같은 레포 특정 지시어를 담은 줄 3개를 요구하고, 막연한 금지 줄만 있으면 부분점수 3점까지만 받습니다. 식별자 없는 정당한 규칙이 0점으로 떨어지지 않게 하기 위한 완충입니다. 반입 전 실측으로 agent 는 92줄 중 50줄, c8c-api 는 378줄 중 242줄이 구체성 기준을 통과해 두 레포 모두 만점 기준 3줄을 크게 웃돌았고 실제 점수는 규칙 단위까지 변동이 없었습니다. 탈락분은 섹션 제목과 막연한 산문뿐입니다. 사용 시점 규칙에는 같은 계단을 두지 않습니다 — 문서당 가이드 하나가 정상이라 반복 표기를 유인하기 때문입니다.
- **0.9.2** — 같은 2회차 검토의 문서 정합 묶음입니다. (1) 생성되는 `.ai-ready/README.md` 의 산출물 표가 산출 디렉터리의 실물 기준이 됩니다 — audit 은 `dashboard.html` 과 `scaffolds/` 를 만들지 않는데(각각 dashboard.py 와 apply 의 몫) 종전 README 가 무조건 표에 넣고 "매 실행 시 갱신"이라 적어, audit 만 재실행한 독자가 낡은 dashboard 점수를 갱신된 것으로 믿게 했습니다. 없는 산출물은 표 대신 생성 방법 안내로 바뀌고, dashboard 는 dashboard.py 를 다시 돌려야 갱신된다고 명시합니다. (2) apply 의 루트 문서 초안 지시가 바이트 기준(800~8,000)으로 바뀝니다 — 채점이 0.8.9 부터 바이트인데 지시만 "50~150줄"로 남아, 0.8.9 의 근거 사례(46줄 = 12,029바이트)처럼 줄 수로는 통과해도 바이트로 초과할 수 있었습니다. (3) MOC 용어를 배점표 첫 등장 자리에서 풀이하고(Map of Content), apply 매핑 표의 닫히지 않는 볼드 마커를 고치고, 0.9.1 채점 노트가 실제 구현보다 넓게 말하던 "hook/CI 명령 실재 확인"을 settings 훅 한정으로 바로잡습니다(CI 는 실행 디렉터리를 알 수 없어 판정 제외).
- **0.9.1** — 2회차 적대 검토(다른 모델의 새 시선)가 실측으로 확정한 우회로들을 막습니다. (1) 루트 문서 경로 참조는 실재하고 스텁이 아닌 대상만 셉니다 — 존재하지 않는 파일을 가리키는 링크 3개로 4/4 를 받았습니다. 깨진 참조는 비고에 나열돼 링크 부패 감지도 겸합니다. (2) 이진 파일이 최소 내용 게이트를 넘지 못합니다(앞 8KB NUL 검사) — 스텁 ADR 옆 4KB 바이너리 하나로 ADR 규칙이 만점이 됐습니다. 디렉터리 게이트는 텍스트 문서 확장자만 셉니다. (3) 갱신 프로토콜 키워드는 실질 문서에 있어야 합니다 — 3줄 스텁의 "갱신 트리거" 한 줄로 5/5 였고, 0.9.0 이 금지·사용 시점 두 형제 규칙만 게이트한 비대칭이었습니다. (4) 훅 명령이 가리키는 레포 상대 스크립트가 없으면 부분점수입니다 — 실재하지 않는 `./gradlew` 와 `freshness_check.py` 를 가리키는 훅 문자열이 검증 훅·신선도 훅 만점을 받았습니다. PATH 명령은 실행 환경을 모르므로 판정하지 않고, 같은 이유로 CI yml 은 이번 판정 대상에서 뺐습니다. (5) config `build_deps` 는 4자 이상이면서 의존성 선언 줄에서만 인정합니다 — 빈 매니페스트의 `plugins {}` 에 "plugins" 를 선언해 2→5 가 됐습니다. config 로 인정된 점수는 리포트 머리에 "config 자기신고 인정" 으로 합산 공개됩니다. 채점당하는 레포가 쓰는 파일이 채점 입력이라는 구조는 유지하되 그 비중을 드러냅니다. (6) antipatterns `doc_hints` 가 스텁 ANTIPATTERNS.md 의 부분점수에 막히지 않고 도달합니다 — naming 힌트와 같은 의미론으로 맞춰, 통합 문서로 관리하는 레포가 낡은 스텁을 지워야만 인정받던 비대칭을 없앱니다. **채점값이 달라져 추이 재기준이 필요합니다** — 실측으로 껍데기 레포 52 → 38(AI 미인지), config 공격 상한 63 → 46(등급 상승 없음), 실제 레포 두 곳은 53 → 53, 71 → 71 로 규칙 단위까지 변동이 없었습니다.
- **0.9.0** — 껍데기 문서로 만점을 받던 구멍을 막습니다. 적대 검토 실측에서 세 줄짜리 스텁 스물한 개로 채운 레포가 100/100 "에이전트 자율" 을 받았습니다. 원인은 둘입니다. 최소 내용 게이트가 비공백 3줄로 느슨했고, 여러 규칙이 그 게이트조차 거치지 않고 파일 존재나 정규식 매칭만 봤습니다. 게이트를 **400바이트 + 비공백 8줄 동시 충족** 으로 올렸습니다. 두 조건을 함께 요구하는 것은 한쪽만 보면 아주 긴 한 줄이나 한 글자짜리 줄 여럿으로 우회되기 때문입니다. 적용 범위도 넓혀 모듈 CLAUDE.md 커버리지, 인덱스, ADR 디렉터리, API 계약 파일, 성과 지표 산출물, 그리고 금지·사용 시점 두 규칙이 새로 게이트를 거칩니다. 금지 가이드는 줄 수로 계단화해 세 줄 이상이라야 만점입니다. 상한만 있고 하한이 없던 두 규칙에 하한을 붙였습니다. 루트 문서는 800바이트 미만이면, 모듈 문서는 평균 10줄 미만이면 부분점수입니다. 종전에는 0바이트 루트 문서와 세 줄짜리 모듈 문서가 만점이었습니다. 규칙을 가리키던 번호(`Rule 2.4` 같은)를 전부 이름으로 바꿨습니다. 배점표에 번호 컬럼이 없어 정의된 좌표계가 없었고 `Rule 1.5` 는 이미 존재하지 않는 번호였습니다. **채점값이 달라져 추이 재기준이 필요합니다** — 실측으로 껍데기 레포는 100 → 52 로 내려갔고, 실제 레포 두 곳은 53 → 53, 71 → 71 로 변동이 없었습니다. 스텁에만 손해가 가는 것이 의도한 비대칭입니다.
- **0.8.9** — 루트 문서 채점 기준을 줄 수에서 **바이트**로 바꿉니다(8,000 이하 만점, 12,000 이하 부분점수). 한국어 마크다운은 한 줄이 표 한 행이거나 문단 하나라 줄 수가 비용을 대변하지 못합니다 — 실측으로 46줄짜리 루트 문서가 12,029바이트(한 불릿만 2,002자)인데 옛 200줄 규칙에서 만점을 받으며 계속 부풀었습니다. 바이트는 한글 3 / 영문 1 이라 토큰 비중에 더 가깝습니다. 근거 문자열에 바이트와 줄 수를 함께 남기고(0점일 때도 남겨) history 에서 추이가 보이게 했습니다. **채점값이 달라져 추이 재기준이 필요합니다** — 옛 규칙에서 만점이던 레포가 낮아지는 것이 의도한 교정입니다(실측 두 레포: 58→53, 74→71). 더불어 `inject_lazy_load_index` 가 "파일명이 곧 트리거" 인 문서(COMMANDS·NAMING·TESTING·ARCHITECTURE·ANTIPATTERNS·INDEX)를 자동 표에서 빼 링크 한 줄로 묶습니다. 0.8.7 이 지운 것은 *중복* 행뿐이라, 중복이 아니면서 디렉터리 목록이 이미 말해 주는 것 이상을 담지 않은 행은 남아 있었습니다. 링크는 묶음 줄에 유지되므로 규칙 1.2 의 경로 참조 수는 줄지 않습니다. 그 규칙은 참조 3건에서 포화해 그 이상은 점수를 주지 않으므로, 긴 트리거 표는 점수를 벌지 못하면서 매 세션 컨텍스트만 씁니다(RUBRIC 채점 노트에 명시). `--json` 사실에 `self_evident` 가 실려 AI 유지보수 경로도 같은 기준을 씁니다.
- **0.8.8** — 루트 문서 `## 모듈 맵` stub 의 모듈 나열 분량을 config 로 정합니다(`module_map.root_stub_limit`, 기본 10 유지). 0 으로 두면 카탈로그 링크만 남고 발췌 나열이 빠집니다. 루트 문서는 매 세션 always-loaded 이고 모듈 한 줄 요약은 그 모듈 CLAUDE.md 에 이미 있어(그 모듈을 열 때 자동 로드) 경로 참조가 다른 데서 충분한 레포에는 발췌가 중복입니다(한 레포에서 약 1,800자). 다만 audit 의 "루트 문서가 3개 이상의 모듈 경로/문서 참조" 규칙이 그 나열을 참조원으로 쓰는 레포도 있어 기본값은 그대로 두고, 0 으로 끄면 점수가 조용히 깎이지 않도록 안내를 출력합니다. 잘못된 값(음수·bool·비정수)은 생성을 멈추지 않고 기본값으로 fallback 합니다.
- **0.8.7** — `inject_lazy_load_index` 가 수동 영역이 이미 가리키는 문서를 자동 표에서 뺍니다. 루트 CLAUDE.md 는 매 세션 always-loaded 라, 같은 문서를 수동 표와 자동 표가 각각 가리키면 그 중복분이 세션마다 컨텍스트를 먹습니다(한 레포에서 12행·약 1,558자 이중 등재). 링크 대상을 후행 슬래시 정규화해 비교하고, 한 행의 대상이 전부 수동 영역에 있을 때만 뺍니다. 감지된 문서가 모두 겹치면 빈 표 대신 안내 문장을 넣습니다. 마커 없는 기존 표를 수동 영역으로 흡수하는 마이그레이션 경로에도 같이 적용돼 흡수 직후 중복이 생기지 않습니다. 지금까지 `config.override_hardcoded` 로 손수 지정해야 했던 일을 자동화한 것입니다.
- **0.8.6** — loop-build 의 phase 통과(PASS) 분기에서 maker 종료를 명시적 절차로 만듭니다. 오케스트레이터가 SendMessage 로 "phase 완료 — 종료" 통지를 보내고 응답을 기다리지 않으며, 그 maker 에는 다시 진입하지 않습니다. loop-maker 행동 규칙에도 통지를 받으면 새 작업 없이 턴을 끝내는 조항이 실립니다. 통과한 phase 의 maker 가 대기 팀메이트로 남던 것을 막습니다.
- **0.8.5** — loop-build 의 maker 를 전용 `loop-maker` 에이전트로 실체화. 행동 규칙(이 phase 만·테스트 동반·설계 결함 시 보고·5줄 보고·커밋 금지)이 스핀 프롬프트에서 에이전트 정의로 옮겨가고, frontmatter `model: opus` 가 기본값으로 실립니다 — 구현은 생산 작업이라 세션 모델 아래 급을 기본으로 두고, 검증은 세션 모델을 상속하는 checker 가 위에 서는 비대칭이 강등의 전제입니다. 호출 시 model 파라미터가 frontmatter 를 이기므로 phase 난도에 따라 상향·하향할 수 있습니다.
- **0.8.4** — loop-checker·loop-lesson-synthesizer 의 frontmatter 모델 고정(opus)을 해제. 판단 에이전트는 호출 세션의 모델을 상속하는 것이 기본이고, 특정 모델이 필요하면 Agent 호출의 model 파라미터로 지정합니다. 메인 모델 세대 교체 때 판단 에이전트가 의도치 않게 강등되는 사고를 막습니다.
- **0.8.3** — 배선·컨텍스트 위생 묶음. (1) freshness Stop 훅이 설치 직후 `CLAUDE_PLUGIN_ROOT` 부재로 죽던 버그 수리 — 훅 셸이 자기 위치에서 파이썬 스크립트를 찾고, audit 가 `.py` 도 함께 복사합니다. (2) checker·synthesizer 에게 컨벤션 문서·지식층·LOCAL rubric 경로를 **프롬프트로** 전달하도록 배선 — 환경변수는 서브에이전트에 전달되지 않아 기존 배선이 끊겨 있었습니다. (3) loop-run 이 brake 값·감지 명령을 `params.env` 로 파일 영속하고 매 Step 이 재유도 — Bash 호출 간 변수 소실로 brake 가 무력화되던 구멍을 막습니다. 재유도 포인터는 브랜치별(`.active-{브랜치}`)이라 한 체크아웃에서 멈춘 루프와 새 루프가 서로 상태를 침범하지 않고, source 는 `set -a` 로 export 까지 복원해 LOCAL rubric 이 채점 자식 프로세스에 전달됩니다. 게이트 실패 재진입도 자기완결 카운터로 brake 가 셉니다. (4) loop-build 의 stall 상태·게이트 카운터를 phase 별 파일로 분리하고, 문서로만 있던 phase 별 history 분리를 `params.env` 재정의로 실제 배선(앞 phase 잔재로 인한 거짓 STALLED 차단). 시간 상한 재계산은 phase 당 원값을 따로 영속해 재개 재실행에도 멱등합니다. (5) 오케스트레이터 컨텍스트 위생 규칙 명문화 — findings 전문을 읽지 않고 채점 요약만 보유, maker 재진입은 scored 파일 경로 인계, phases.json 갱신은 jq, 설계 세션과 오케스트레이션 세션 분리 권고. (6) decide/stall 입력 계약 fail-loud 검증, npm 기본 test 스텁 미채택, loop-review 베이스 브랜치 감지 순서·findings 경로 격리, BASE rubric 의 c8c-api 특화 security 좁히기를 LOCAL 결정으로 명시, 문서 드리프트 다수 정정.
- **0.8.2** — checker findings 를 오케스트레이터가 지정한 결정적 파일로 회수합니다. 백그라운드 세션에선 서브에이전트의 최종 메시지가 인라인으로 전달되지 않아, 파일이 유일한 회수 경로입니다. checker 의 쓰기 금지 규칙에 그 단일 경로 예외를 두고, 스핀 전 파일 비우기와 빈 파일 fail-loud 가드로 stale 결과·미기입이 통과로 둔갑하는 것을 막습니다.
- **0.8.1** — loop-build 견고성. 무인 순회를 시작·재개하기 직전 `phases.json` 을 검증해, 손상된 분해(빈 phase 목록, 이름·step·완료 확인 명령 누락, 잘못된 상태 값)면 즉시 멈추고 사람을 부릅니다. 기존 loop-run 등 사용처와 격리돼 하위호환을 깨지 않습니다.
- **0.8.0** — `loop-build` 신설(멀티-phase 무인 빌드아웃, phase 별 서브에이전트 maker). 루프 상한 기본값 조정(회차 10→5, 시간 60→120분, 비용 $100→$500, 토큰 1M→5M). loop-run 문서 정정.
- **0.6.0** — 과거 별도 plugin 이던 무인 검증 루프(loop-engine)를 ai-ready 안으로 통합. `/loop-run`·`/loop-review`·`/loop-lessons` + `loop-checker`·`loop-lesson-synthesizer` 에이전트 + 결정론 채점 셸이 audit/apply 와 한 plugin 으로 배포됩니다. 하나만 설치하면 `audit→apply→verify` 전 주기를 씁니다.
- **0.5.0** — apply 가 문서를 외과적으로 유지보수(사실만 모으는 `--json` 모드 + AI 부분 수정 + 사람 큐레이션 보존). 통째 덮어쓰기 폐기.
- **0.4.0** — 사람이 인수한 문서 보호(자동 생성 서명 없으면 덮어쓰기 거부, `--force` 우회, 문서별 diff 확인 루프).
- **0.3.0** — `.ai-ready/config.json` 의 rubric 섹션으로 채점이 프로젝트 현실을 반영(통합 design 디렉터리를 ADR 로, springdoc 를 API 계약으로 인정 등).
- **0.2.0** — `.ai-ready/config.json` 으로 frontmatter 인식 INDEX 그룹화와 사용자 lazy-load 트리거 보존.

<details>
<summary>더 이전 버전</summary>

- **0.7.0** — 5관점 적대 리뷰 기반 보강. 채점 정직화(빈/스텁 문서 부분점수, half-credit 공식 정합), 무인 루프 안전(베이스 ref·빈 diff 거짓 PASS 가드, checker/synthesizer 쓰기 금지 명문화), 배포 위생(freshness 훅 경로 통일, LICENSE 추가), 스크립트 견고성(BOM·타입 가드 등). **채점값이 일부 달라져 추이 재기준이 필요합니다.**
- **0.6.1** — loop 스킬 3개의 `disable-model-invocation` 제거(모델 자동 트리거에도 노출). 기능 변화 없음.
- **0.4.2** — INDEX 생성이 `.gitignore` 를 존중(추적·미무시 `.md` 만 인덱싱, 비-git 디렉터리는 전체 walk 로 폴백).
- **0.4.1** — 생성 스크립트 전반 버그 수정 묶음(worktree 오염으로 모듈·문서가 부풀던 문제 등 30건).
- **0.1.2** — manifest 스키마 수정, 단일 모듈 프로젝트 평가·스캐폴드 분기, thin-index 인식, 추이 스파크라인, ANTIPATTERNS 클러스터링.

</details>

---

## 라이선스

MIT
