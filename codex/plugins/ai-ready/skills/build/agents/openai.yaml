interface:
  display_name: "AI-Ready Build"
  short_description: "설계를 phase 로 쪼개 무인 maker/checker 루프로 PASS 까지 구현합니다"
  default_prompt: "Use $build to implement an approved design across phases with an unattended maker/checker loop."
