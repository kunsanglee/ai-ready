interface:
  display_name: "AI-Ready Loop Run"
  short_description: "단일 변경을 무인 maker/checker 루프로 PASS 까지 수렴시킵니다"
  default_prompt: "Use $loop-run to converge the current change to a PASS verdict with an unattended maker/checker loop."
