# 변경 이력 — ai-ready

이 플러그인이 무엇이고 어떻게 쓰는지는 [README.md](README.md) 에 있습니다. 이 파일은 릴리스마다
무엇이 왜 바뀌었는지만 담습니다. 최신 릴리스가 맨 위입니다.

릴리스 이력의 단일 출처가 이 파일입니다 — 매니페스트 `description` 에 쌓지 않습니다(그 필드는
플러그인 목록에서 사람이 읽는 한 문단이고, 이력을 덧붙이다 19,805자까지 부푼 적이 있습니다).
`build/drift-test.sh` 가 두 가지를 검사합니다. 매니페스트 넷의 버전이 같은지, 그리고 그 버전의
항목이 이 파일에 있는지.

- **1.5.6** — **렌즈별 소요 시간과 점검 범위 크기를 회차 이력에 계측으로 남깁니다(판정 무관).** 조건별 checker 팬아웃(변경이 크면 렌즈를 나눠 병렬로 읽히는 개선)의 전제는 "회차 벽시계의 병목이 checker 읽기이고 렌즈 시간이 읽는 양에 비례한다" 인데, 이것을 렌즈별로 잰 적이 없습니다. 측정 장치 없이 넣은 휴리스틱이 운영 내내 발동 0 으로 죽어 있던 전례를 따라, 설계 전에 재는 장치를 먼저 둡니다. 방법은 결정론입니다 — 렌즈를 띄우는 주체가 LLM 이라 시계를 맡길 수 없어, 회차 스냅숏 파일(렌즈 직전 기록)의 mtime 을 시작점으로, 각 렌즈가 마지막에 쓰는 산출 파일의 mtime 을 끝점으로 파일시스템이 잽니다. 오케스트레이터의 프롬프트 조립 시간이 앞에 끼므로 상한값입니다. history 줄에 `lens_seconds`(렌즈별 초)와 `scope_files`(렌즈에 넘긴 파일 수, 전 범위면 "full")가 붙고, 창에는 "렌즈 소요(계측, 판정 무관)" 한 줄이 나옵니다. 계측 실패(스냅숏 없음·구판에서 시작한 실행)는 빈 값으로 지나가 회차를 막지 않습니다. 다음 실전 루프부터 데이터가 쌓이고, 팬아웃 설계는 그 실측이 전제를 확인한 뒤 별도 스펙으로 갑니다.
- **1.5.5** — **셋째 게이트 슬롯을 `QUALITY` 로 개명해 기본 사슬에 세우고, 파서가 복잡도 도구 출력을 항목으로 쪼갤 수 있게 합니다.** 외부 대조에서 "복잡도를 보는 자리가 없다" 는 지적이 나왔는데, 실물을 열어 보니 셋째 슬롯(`run_gate LINT` + `LOOP_LINT_CMD`)이 이미 있었습니다. 다만 산문의 옵트인으로만 존재해 실행 사슬에 없었고, 비어 있던 것은 파서의 어휘였습니다. 게이트를 새로 만들지 않고 그 둘을 고쳤습니다. **① 슬롯이 실행 사슬에 섭니다**: `run_gate BUILD && run_gate TEST && run_gate QUALITY` — 명령이 비면 스킵을 소리 내고 통과해 "안 켠 것" 과 "통과한 것" 을 가릅니다. 사슬 편입은 시험 3개와 제거 변이 1개로 잠갔습니다(산문에만 있고 사슬에 없던 상태로 되돌리면 셋 다 실패). **② 이름을 실물에 맞춥니다**: 이 슬롯이 싣는 것은 린트만이 아니라 복잡도·정적 분석 전반이라 `LOOP_QUALITY_CMD`/`quality_cmd`/`run_gate QUALITY` 로 통일했습니다. 하위 호환 별칭은 없습니다 — 옛 이름 `LOOP_LINT_CMD` 를 쓰던 셋업은 이름만 바꾸면 되고, 안 바꾸면 그 지정값은 경고 없이 무시되고 감지값 또는 스킵으로 떨어집니다. 1.5.4 이하로 시작해 진행 중이던 실행을 재개하면 params.env 에 옛 키만 있어 QUALITY 는 스킵(stderr 표시)으로 돕니다. **③ 파서가 복잡도 도구의 실측 형식 셋을 압니다**: eslint 10.9.0 stylish(@ 스코프 규칙 이름, 규칙 열이 없는 구문 오류는 `compile-error` 로, `--max-warnings` 초과 실패에서는 경고도 항목으로), radon cc 6.0.1, xenon 0.9.3 의 기본 block 판정. eslint `complexity` 규칙과 radon·xenon 판정은 `complexity-over-threshold` kind 로 큐에 실리고, maker 큐 순서 계약(`agents/loop-maker.md`·codex `maker-role.md`)에 그 자리가 생겼습니다. 단 radon cc 는 위반이 있어도 종료코드 0 이라 단독으로는 게이트를 못 깹니다 — 문턱 실패는 xenon 같은 종료코드 도구의 몫입니다. 덤으로 둘: 게이트 출력의 ANSI 색을 매칭 전에 벗깁니다 — 도구 성질이 아니라 FORCE_COLOR 를 내보내는 에이전트 하네스 환경의 성질입니다(FORCE_COLOR=3 의 eslint 실측, 변수를 지우면 색이 없음). 그리고 중복 제거 키가 존재하지 않는 필드명을 읽어 줄 번호를 안 보던 결함을 고쳐, 같은 메시지가 다른 줄에 나면 별개 항목입니다. 파서 회귀 시험 24→37건.
- **1.5.4** — **모듈 `CLAUDE.md` 초안 옆에 `AGENTS.md` 상대 심링크를 같이 둡니다.** 스캐폴드가 `CLAUDE.md` 만 쓰면 Codex 처럼 그 이름만 찾는 도구는 빈 폴더로 봅니다. 복사본을 두면 한쪽만 고치고 다른 쪽이 낡고, 루트 문서 둘을 각각 재는 채점(v1.5.3)에서는 크기가 갈립니다. 상대 심링크는 본문이 하나라 그 둘이 안 생깁니다. 사람이 이미 쓴 `AGENTS.md` 는 덮지 않습니다. 검사가 심링크 대상·기존 파일 보존·이미 있는 심링크 무작업을 고정합니다.
- **1.5.3** — **같은 방식의 감사를 audit·apply 스킬로 넓혀, 반박 검증을 통과한 지적 여덟을 닫습니다** (서로 모르는 finder 다섯 + 지적별 회의론자 여덟, 30건 중 8건 확정·기각 0). 절반은 실패가 성공으로 보고되던 자리입니다. **① git 을 못 읽으면 문서를 만들지 않습니다**: `extract_antipatterns.py` 가 git 부재·비 git 디렉토리·타임아웃을 전부 빈 결과로 삼켜, 히스토리를 한 줄도 읽지 못한 채 "저장소가 건강하거나…" 라는 시드를 exit 0 으로 냈습니다(재현으로 확정). 이제 진입 시 `rev-parse` 로 확인해 실패면 stderr 안내 + exit 4(`EXIT_GIT_UNAVAILABLE` — 3 은 덮어쓰기 거부가 이미 쓰므로 다음 번호)로 멈추고, 커밋 0개의 신생 저장소만 정당한 "히스토리 없음" 으로 진행합니다. **② history 보관 실패도 무음이었습니다**: 보관이 실패해도 매 실행이 `archive:` 줄을 찍어, 몇 달 뒤 추이 그래프를 열 때에야 데이터가 없음을 알게 됩니다. 이제 실패는 stderr 경고를 남기고 성공했을 때만 그 줄이 나옵니다(채점 자체는 계속, exit 0). **③ 비 JVM 단일 모듈이 만점에 닿지 못했습니다**: 문서 세 곳이 "카탈로그만으로 만점" 을 약속하는데 코드는 4/5 에서 멈춰, node·python·go·rust 저장소는 최대 99점에 controller/service 레이아웃 액션이 영구히 남았습니다. 레이아웃 개념이 없는 스택은 5점, JVM 인데 Controller 도메인이 없는 경우만 4점으로 갈랐습니다. **④ 없는 파일을 언급만 해도 만점이던 갈래**: 루트 문서에 "docs/PACKAGES.md 를 만들 예정" 한 줄만 있어도 카탈로그 참조 규칙이 4/4 를 줬습니다(경로 갈래만 죽은 참조를 거르고 카탈로그 갈래는 안 걸렀습니다). 실재 + 스텁 게이트를 같은 기준으로 겁니다. **⑤ 루트 문서 채점이 파일시스템 순서에 달려 있었습니다**: 루트에 CLAUDE.md 와 AGENTS.md 가 같이 있으면 readdir 순서로 먼저 잡힌 하나만 재서, 얇은 파일이 먼저 잡히면 20KB 문서가 아예 안 재져 만점이 됐고 머신에 따라 0점도 됐습니다. 파일 수집을 이름 정렬로 결정론화하고, 두 루트 문서를 각각 재어 나쁜 쪽을 규칙 점수로 삼습니다(합산이 아닌 이유: 한쪽이 다른 쪽의 거울인 구성에서 같은 내용을 두 번 세게 됩니다). **⑥ codex 거울의 배점표가 두 판 낡아** 코드는 맞고 문서만 거짓이었습니다 — claude 쪽을 바이트 복사하고 동일성 테스트로 고정했습니다. **⑦ apply 매핑 표와 규칙 이름의 일치를 검사가 지킵니다**: 규칙 이름 두 개가 조절 상수를 이름에 내장해 이미 두 번 바뀌었는데 표를 지키는 검사가 없었습니다. 두 레이아웃의 규칙 이름 전체와 표 첫 칸의 집합 동일성을 단언합니다. **⑧ drift-test 가 audit 스크립트 층도 잠급니다**: 엔진 셸만 잠그고 audit 사본 13개는 손 동기화에 맡겨져 있었습니다. 의도적 차이만 비켜 갑니다 — audit.py 는 어댑터 분기, install_hook.py 는 codex 번들에 일부러 없고, freshness_check.py 는 codex 에서 자리가 달라(freshness 스킬 아래) 별도 줄로 바이트 비교합니다. 전부 변이(고침을 되돌려 검사 실패 확인)로 잠겼고, 검사는 236건 + codex 12건 + 엔진 184건 통과입니다. **서로 모르는 리뷰어 셋(정합·운영·문장)이 이 반영을 먼저 봤고 지적을 추가로 닫았습니다**: 1.5.2 의 회차 좁히기가 남긴 스냅숏이 다음 실행의 첫 회차를 조용히 좁히던 것(초기화 목록에 추가), 좁히기가 "넓게 가기로 한 결정"(마지막 phase·review_scope full·재개)을 덮던 것, 좁힌 회차의 PASS 가 마지막 허용 회차에 나오면 확인 회차를 못 돌던 것(PASS 를 brake 보다 먼저 보고, 확인 회차 한 번은 회차 상한을 넘어설 수 있다 — 절대 상한·시간 상한은 그대로), 위 ⑧ 이 audit.py 를 제외해 채점 로직이 검사 밖이던 것(의도적 어댑터 구간만 마커로 표시하고 나머지를 바이트 비교), `detect_build.py` 폴백에서 `check` 제거(포맷 검사가 컴파일 게이트로 잡히는 것을 실측), 참조를 세는 규칙이 루트 문서 둘 중 첫 파일만 읽던 것(합집합으로), 1.5.2 의 게이트 0개 거부·회차 좁히기 계약을 셸 블록 검사로 고정. 반영을 다시 리뷰해 둘을 더 닫았습니다 — 잔재 정리를 셸 글롭 rm 에서 find 로 바꿨고(zsh 는 매칭 없는 글롭 하나에 명령 전체를 취소해 잔재가 하나도 안 지워진 채 종료코드 0 이었습니다. 실측. 검사에 zsh 축 추가), 확인 회차 예외는 상한 비교를 건너뛰는 대신 상한을 정확히 한 칸 올립니다(게이트 실패가 이어지면 마커가 살아남아 예외가 되풀이되던 것을 실측). **문서 다이어트도 함께 합니다**: 에이전트에게 로드되는 문서 전체를 문장 단위로 분류해, 지워도 다음 행동이 달라지지 않는 재서술·죽은 이관 안내·자기 서술 라벨을 걷어냈습니다. checker 정의는 특정 스택 예시 목록을 내리고(권위는 처음부터 프로젝트 컨벤션 문서와 rubric), BASE rubric 은 렌즈와 무관한 운영 산문을 엔진 README 로 옮겼으며(둘 다 회차마다 렌즈 셋이 읽어 3배로 곱해지는 자리), build 스킬은 트러블슈팅 표를 실패 시에만 읽는 `references/troubleshooting.md` 로 옮겼습니다. 생성 템플릿(스캐폴드·주입 스크립트)의 자기 서술·반복 안내도 걷어 대상 저장소의 상시 문서가 가벼워집니다. 모듈 CLAUDE.md 골격에서 의존성·최종 검토일 절도 뺐습니다 — 의존성은 빌드 매니페스트가, 검토 이력은 git 이 정본이라 문서에 복사하면 낡는 순간 거짓이 됩니다(c8c-api 의 문서 문장 규약과 왕복 검증으로 확인 — 재생성이 지운 문장을 되살리지 않습니다). 실사고 날짜가 붙은 실측 기록과 검사가 고정한 문장은 전부 남겼습니다.
- **1.5.2** — **다섯 시선의 감사에서 반박 검증을 통과한 지적 다섯을 닫습니다** (서로 모르는 finder 다섯 + 지적별 회의론자 여덟, 34건 중 5건 확정). **① 채점 안 된 입력이 PASS 로 둔갑하던 구멍**: `decide.sh` 가 severity 없는 finding 을 조용히 걸러내, score 를 안 거친 checker/merge 출력이 배선 실수로 직결되면 counts 0 → PASS 가 됐습니다(재현으로 확정 — 보안 지적 20건이 들어도 통과). 이제 severity 가 없거나 사다리 밖(LOCAL 표 오타)이면 exit 65 로 멈춥니다. 엔진 검사 두 건이 이 동작을 고정하고, 변이(옛 select 복원)로 실패를 확인했습니다. **② 게이트 명령이 둘 다 비면 시작하지 않습니다**: 빈 명령은 게이트를 스킵하므로, 감지 실패 시 컴파일·테스트가 한 번도 안 돈 코드가 렌즈 판정만으로 PASS 까지 갈 수 있었습니다. 없는 것과 못 찾은 것을 가릅니다 — 정말 게이트가 없는 대상(문서 전용)은 `LOOP_NO_GATE=1` 명시 선언으로만 진행하고, 사람이 미리 지정한 명령이 감지값을 이기며(트러블슈팅 표의 우회가 실제로 서는 자리), `detect_build.py` 는 npm 의 build 스크립트가 없으면 typecheck·tsc·compile·check 를 순서대로 대신 집습니다(실측 사고가 typecheck 이름이었습니다). **③ 등급표가 자라는 경로가 구조적으로 닫혀 있던 것**: "기록된 severity 가 floor 와 다르면 예외표 후보를 낸다" 는 조건에서, 미등록 종류의 기록값은 floor 채점의 복사본이라 이 비교가 상수 대 자기 자신이었고, 실제로 지금 예외표 항목은 전부 사람이 오프라인으로 넣었습니다. 재설계 없이 조건식만 바꿉니다 — 표에 없는 종류가 반복되면 synthesizer 가 적정 base_severity 를 스스로 따져 후보를 내고(기준: 통과를 막을 가치가 있나, 처방이 코드인가 문구인가), 개별 finding 재채점 금지는 그대로 두되 표 초안의 권장 등급은 다음 실행의 정책 제안이라 예외임을 명시합니다. 결정론 채점·사람 승인 게이트·BASE/LOCAL 분리는 그대로입니다. **④ 회차 좁히기**: phase 진입 스냅숏은 phase 사이 누적만 잘라, 한 phase 안에서는 렌즈가 매 회차 같은 것을 다시 읽었습니다(실측: checker 가 회차 벽시계의 35% — 2026-08-16, agent-ts). 이제 2회차부터 "직전 렌즈 실행 이후 바뀐 파일 + 직전 회차 지적이 가리킨 파일"만 넘깁니다 — 합집합이 요점입니다. 지적 파일을 빼면 안 고친 지적이 시야에서 사라져 고쳐진 것과 구분이 안 됩니다. 실패는 전부 넓은 쪽으로 떨어지고, **좁힌 회차의 PASS 는 phase 범위 확인 회차를 한 번 돈 뒤에만 닫습니다**(마지막 시야는 넓게). **⑤ 실행 형태 안내**: 완료 조건이 전부 "되돌리면 X 가 실패한다" 로 열거된 작업은 루프의 적응력이 필요 없어 조건별 병렬 워크플로가 빠릅니다(실측: 같은 단계를 루프 회차당 46분 × 4~5회차 vs 워크플로 6분). spec-checker 가 조건 형태 문제를 지적하지 않았으면 승인 화면에 이 갈림을 한 줄로 알리고 사람이 고릅니다 — 강제하지 않습니다. 전부 codex 트리에도 같은 자리에 적었고, 엔진 셸 세 사본은 바이트 동일입니다.
- **1.5.1** — **maker 가 넘기기 전에 자기 수정을 되돌려 확인하고, 주석은 코드가 말하지 못하는 것만 남깁니다.** 실측이 근거입니다(2026-08-16, agent-ts 16단계 이관): 한 phase 의 4회차에서 통과를 막은 지적 셋이 **전부 앞 회차 수정의 잔여물**이었습니다 — 막으려 더한 방어의 조건이 정작 실패하는 순간에 거짓이었고, 상한 값은 넣었는데 되돌려도 아무것도 실패하지 않았습니다. 셋 다 "고쳤다고 하고 실패 경로를 한 번도 실행해 보지 않은 것"이라, maker 안에서 짧게 끝났을 확인이 45분짜리 회차 하나로 돌아왔습니다. 이제 `loop-maker` 계약에 이렇게 적습니다: **프로덕션 코드의 동작을 바꾸는 수정마다 사본에서 되돌려 무엇이 실패하는지 이름으로 확인하고**(`mktemp -d` 임시 폴더에 복사 — 워킹 트리에는 앞 회차 수정이 커밋 없이 쌓여 있어 git 으로 되돌리면 같이 되돌아갑니다), 아무것도 실패하지 않으면 **그 수정을 되돌리면 실패하는 검사**를 먼저 씁니다. 이를 위해 "테스트는 게이트의 몫" 규칙에 예외를 하나 열었습니다 — 되돌림 확인만은 게이트가 쓰는 테스트 명령을 그 수정 범위로 좁혀 스스로 돌리고, 그 명령과 **확인 기록 경로**(`maker-revert-{phase}.jsonl`)를 오케스트레이터가 프롬프트로 넘깁니다. 확인은 보고 한 줄이 아니라 그 파일에 남아, 사람이 brake 나 AWAIT_USER 로 불려 왔을 때 여는 근거가 됩니다. 주석·문서·이름 변경, 코드 삭제, 테스트만 더하는 수정은 대상이 아닙니다 — 되돌려도 실패할 검사가 원리상 없거나(주석·문서·이름), 처방이 "그 테스트를 잡는 테스트" 라는 순환이 됩니다(테스트 추가). **주석 계약도 함께 넣었습니다**: 같은 실측에서 새로 쓴 파일 넷의 토큰 75%가 주석이었고, 주석 지적(`comment-noise`·`comment-rot`)은 MINOR 라 통과를 안 막아 매 회차 checker 자리만 차지하며 다시 나왔습니다. 이제 maker 는 **코드가 말하지 못하는 것**(알면서 남긴 한계, 비직관적 선택의 이유)만 주석으로 남기고, 주석 지적을 받으면 고쳐 쓰기보다 **지우는 것을 기본**으로 합니다 — 고쳐 쓴 주석은 또 틀려질 수 있지만 지운 주석은 그럴 수 없습니다. rubric 이 `comment-rot` 의 예외로 둔 셋(문장 안에서 다 세어지는 개수·날짜 붙은 실측 기록·검사가 지키는 사실)은 지우지 않고 오탐으로 보고하며, 프로젝트 컨벤션이 주석 형식을 의무로 정했으면 그쪽이 우선합니다. 겸해서 `comment-rot` 을 checker 계약의 convention 종류 목록에 올렸습니다 — 1.4.2 가 rubric 표에만 등록하고 계약 본문에는 안 올려, 목록과 표가 다른 답을 하고 있었습니다. codex 트리의 maker 계약(`codex/plugins/ai-ready/skills/build/references/maker-role.md`)에도 같은 규칙을 같은 자리에 적었습니다. **서로 모르는 리뷰어 셋(정합성·운영·문장 규율)이 이 변경을 먼저 봤고**, 검사 명령 미전달·확인 무흔적·사본 경로 충돌·codex 쪽에 없는 규약 인용을 포함해 지적 열둘을 반영했습니다.
- **1.5.0** — **렌즈가 읽는 범위를 이 phase 가 실제로 만든 것으로 좁힙니다(`review_scope.py`).** 비교 베이스는 Step 0 에서 한 번 정해지고 순회 내내 그대로라, **렌즈가 보는 양이 phase 가 진행될수록 자라기만 합니다.** 실측이 근거입니다 — 앞 저장소의 한 phase 가 여덟 회차에 612분을 썼고 회차 하나가 평균 76분인데, **그중 게이트는 14초입니다**(전량 검사를 컨테이너째 콜드로 다시 세워도 그렇습니다). 나머지가 통째로 렌즈가 누적된 변경을 읽는 시간이고, **여덟째 회차의 렌즈도 첫 회차와 같은 2,600줄을 여덟 번째로 읽었습니다.** 이제 phase 에 들어갈 때 변경 상태를 파일별 해시로 적어 두고(`snapshot`), 렌즈를 띄우기 직전에 그 이후 내용이 달라진 파일만 뽑아(`since`) 프롬프트에 싣습니다. **경로 목록이 아니라 내용 해시로 재는 것이 이 변경의 핵심입니다** — 목록으로 좁히면 *앞 phase 가 만든 파일을 이번 phase 가 고친* 자리가 제외 대상으로 떨어져 그 변경이 통째로 안 읽히는데, **그렇게 돼도 아무 검사가 실패하지 않습니다.** 그래서 그 성질을 검사 이름으로 박아 뒀습니다(`test_review_scope.py` 의 "앞 phase 파일 수정이 잡힌다"). **좁히기는 안전한 쪽으로 떨어집니다** — 스냅숏이 없거나 목록이 비면 좁히지 않고 전 범위를 넘깁니다. 빈 목록과 "아무것도 안 고쳤다" 가 같은 값이라 넓은 쪽이 맞고, 좁히기가 실패하면 느려질 뿐 헐거워지지 않습니다. **대가를 갚는 자리를 하나 남깁니다** — 좁히면 phase 끼리 부딪히는 결함을 아무도 안 보게 되므로 **마지막 phase 는 적든 안 적든 안 좁히고**, 중간 phase 도 `review_scope: "full"` 로 그렇게 만들 수 있습니다(되돌림 확인처럼 전 범위를 다시 도는 phase 가 그 자리입니다). **커밋도 ref 도 안 만듭니다** — "루프는 커밋하지 않는다" 규약을 그대로 둔 채 작업 트리를 읽기만 합니다. **개발 중에 이 검사가 실제로 버그를 하나 잡았습니다**: 이 phase 가 만들었다가 지운 미추적 파일은 HEAD 에도 미추적 목록에도 안 남아 흔적이 스냅숏뿐인데, 처음 구현이 현재 상태만 훑어 그 삭제를 놓쳤습니다(주석은 "삭제는 이미 잡힌다" 고 적고 있었고 그건 추적 중인 파일에만 맞았습니다). 단언 13개를 두고 **변이 일곱을 각각 확인했습니다** — 커밋된 변경 출처 제거·미추적 출처 제거·무시 제외 플래그 제거·해시 대신 경로 비교·삭제 합집합 되돌림·스냅숏 구분자를 개행으로·스냅숏 읽기를 줄 단위로. 일곱 다 실패하고 되돌리면 통과합니다. **codex 트리에도 같은 규약을 적었습니다** — 그쪽은 셸 블록이 없어 산문이 계약의 전부이고, 스크립트 자체는 `core/_loop-engine` 단일 원본이라 양 트리가 바이트 동일 사본으로 갑니다.
  **서로 모르는 리뷰어에게 이 변경을 넘겨 다섯을 고쳤고, 그중 하나가 이 릴리스가 약속한 문장을 거짓으로 만들고 있었습니다.** 위에 "목록이 비면 전 범위로 넘긴다" 를 적었는데 **그 분기가 도달 불가였습니다.** `grep -c` 는 일치가 하나도 없으면 `0` 을 찍고 **종료코드 1** 로 끝나서 `|| echo 0` 이 한 번 더 찍고, 값이 `0\n0` 이 되어 정수 비교가 죽고, 하필 조건이 거짓이 되어 **빈 목록이 그대로 렌즈로 넘어갔습니다**(실측으로 재현했습니다). 세는 값이 안내 문구에만 쓰이는데 그것 때문에 판정이 갈리던 자리라, 세기를 없애고 `[ -z "$SCOPE" ]` 로 봅니다 — 중간 파일과 `rm -f` 둘도 함께 사라졌습니다. **그 버그 자체가 조용해서 같은 패턴을 잡는 검사를 조립 검사에 넣었고**(`build/drift-test.sh` 의 `[grep-c]`), 일부러 다시 넣어 잡히는 것을 확인했습니다. **둘째는 되살림을 놓치던 것입니다** — 진입 시점에 이미 지워져 있던 추적 파일을 이번 phase 가 원래 내용으로 되살리면 base 대비 무변경이 되어 현재 목록에서 빠지는데, 없는 쪽 기본값을 `-` 로 둬서 스냅숏의 `-` 와 같아져 안 바뀐 것으로 떨어졌습니다. 기본값을 없애고 단언을 하나 더 뒀습니다(변이로 확인). 나머지 셋은 **호출자가 자기 테스트뿐인 출력 모드 제거**(지금 요구에 없는 확장 포인트였습니다), **틀린 수를 찍던 안내 문구 제거**(이 루프는 커밋하지 않아 그 수가 정상 실행에서 늘 0 이고 같은 `grep -c` 버그까지 갖고 있었습니다), **존재하지 않는 검사를 가리키던 주석 교정**입니다(마지막 phase 를 전 범위로 돌리는 것은 검사가 아니라 셸입니다).
  **축을 셋으로 갈라 리뷰했고 나머지 두 축이 아홉을 더 냈습니다. 이번에도 절반이 "되돌려도 아무것도 실패 안 하는" 자리였습니다.** 가장 무거운 것은 **재개가 진입 스냅숏을 덮어써 그 phase 가 중단 전까지 만든 것을 통째로 범위 밖으로 내보내던 것**입니다(실측: 파일 셋 → 하나). 진입 블록은 재개로도 다시 도는데 조건 없이 덮고 있었고, 하필 사람이 끼어든 phase 라 가장 봐야 할 작업이 안 보였습니다. 한 번만 찍게 가드를 걸고 Step 0 초기화 대상에 스냅숏을 더했습니다(둘은 짝입니다 — 가드만 걸면 같은 티켓 재시작 때 옛 스냅숏이 살아 있는 값이 됩니다). **둘째는 `review_scope` 값을 아무도 검증하지 않던 것**입니다. `jq` 의 `//` 가 `false` 를 빈 값으로 봐서 그 값이 조용히 "좁힘" 으로 떨어지는데, **바로 옆 `non_goals` 는 `false` 가 "안 좁힘"** 이라 그 관용을 옮겨 적으면 정확히 반대를 얻습니다. `"FULL"`·`"all"` 도 같은 방향이었습니다 — 착수 전 검사 두 곳이 이제 어휘 밖을 거부합니다. **셋째는 배선에 단언이 하나도 없던 것**입니다. 진입 스냅숏과 점검 범위 블록을 통째로 지워도 검사 전부가 통과했습니다. 스킬 블록 단언 다섯과 codex 계약 단언 하나를 더해 변이 열로 확인했습니다 — 그 codex 단언을 **처음에 개수로 셌다가 한 자리 삭제를 놓쳐** 위치별로 고쳤습니다(이 릴리스가 고치는 것과 같은 종류의 실수입니다). **그리고 스크립트가 자기 불변식을 마지막 한 홉에서 놓고 있었습니다** — `-z` 와 NUL 로 개행 든 경로를 지켜 놓고 출력은 줄 단위라, 그런 경로 하나가 **없는 경로 두 개로 쪼개지고 정작 바뀐 파일은 목록에서 사라졌습니다.** 이제 그 경우 좁히기를 포기하고 exit 3 으로 알립니다(부르는 쪽은 0 이 아니면 전 범위로 떨어집니다). **`--root` 가 저장소 루트가 아니면 거부합니다** — `git diff` 는 저장소 루트 기준 경로를 주는데 해시는 그 root 기준으로 열어서, 하위 디렉터리를 주면 변경이 통째로 "지워짐" 으로 접히고 스냅숏과 비교값이 같아 **영영 범위 밖에 남았습니다.** 같은 자리에서 "못 읽음" 과 "사라짐" 도 갈랐습니다. 나머지는 스냅숏 부재·미완주·phase 하나 경우의 안내와, codex 트리의 필드 열거 두 곳, 렌즈 계약 두 벌의 `reviewed` 관계입니다. **사람을 부를 때 "아직 전 범위를 본 phase 가 없다" 를 함께 올리게 했습니다** — 마지막 phase 예외는 순회를 끝까지 돌아야 오는데 그 전에 멈추면 그 자리가 한 번도 안 돕니다.
  **곁다리로 변이 하네스 하나를 고쳤습니다.** `mutate_skill_blocks.py` 가 찾는 원문이 `non_goals` 도입 때 문서에서 "셋" 이 빠진 것을 안 따라가, 그 변이가 **적용도 안 된 채 "살아남았다" 로 보고**되고 있었습니다(1.4.2 에서도 같았습니다). 진짜 생존자를 가리는 모양이라 함께 고쳤고, 이제 일곱 전부 잡습니다.
  **업그레이드 시 확인할 것 하나** — 올린 뒤 **세션을 새로 시작하십시오.** 스킬 문서는 클론의 현재 브랜치에서 읽히는데 진행 중이던 루프의 `.loop/run/` 에는 phase 진입 스냅숏이 없습니다. 그 상태로 재개하면 **그 phase 는 끝까지 전 범위로 돕니다** — 진입 블록이 "이력은 있는데 스냅숏이 없다" 를 업그레이드 중 재개로 읽고 새로 찍지 않습니다. 지금 찍으면 업그레이드 전에 만든 것이 통째로 범위 밖이 되기 때문입니다. 좁히기는 **다음 phase 진입부터** 걸립니다.
- **1.4.2** — **주석이 자기 파일 밖 사실을 말하는 자리를 잡는 종류를 더합니다(`comment-rot`, convention/MINOR).** 지금까지 주석을 보던 종류는 둘이었습니다. `comment-noise` 는 코드를 그대로 다시 말하는 중복을 보고, `doc-lags-code` 는 문서가 코드에 뒤처진 것을 봅니다. **그 파일이 안 바뀌어도 틀려지는 주석은 어느 쪽도 아닙니다** — 다른 컴포넌트의 동작이나 근거가 다른 파일에 있는 개수를 적으면, 그 값이 바뀔 때 이 파일은 안 열리므로 아무도 모르고 컴파일과 테스트는 그대로 통과합니다. 개명할 이름이 없어 grep 규율에도 안 걸립니다. **등급을 MINOR 로 둔 것이 이 변경의 핵심입니다.** 종료 판정이 "MINOR 만이면 Pass" 라 MAJOR 이상이면 주석 문구를 고치라고 되돌려 보냅니다. 행이 없으면 dimension floor 로 채점되는데 convention 만 MINOR 이고 simplicity·intent 는 MAJOR 이며 여섯 어휘 밖 차원은 CRITICAL 이라, **행이 빠진 상태에서는 통과 여부가 checker 의 태깅에 달립니다.** 형제 둘이 각자 행을 받아 둔 것과 같은 이유입니다. **대상이 아닌 셋을 함께 적습니다.** 그 문장 안에서 다 세어지는 개수("다른 점 둘. (1)… (2)…"), 날짜를 붙인 실측 기록(기록은 낡아도 거짓이 아니지만 판정 기준은 낡으면 거짓이 됩니다), 그리고 **검사가 지키고 있는 사실**입니다. 셋째는 실측에서 나왔습니다 — 다른 파일의 동작을 적은 주석인데 같은 이름의 테스트가 그 성질을 검증하고 있어, 바뀌면 조용히 틀려지는 대신 테스트가 먼저 실패합니다. 프로젝트가 주석 속 경로의 실재를 빌드에서 검사한다면 경로도 이 셋째에 듭니다(그런 저장소에서는 경로를 **적는 것이** 규약입니다). 예외를 안 적으면 checker 가 안 썩는 주석까지 셉니다. 실제로 후보 목록을 손으로 열어 보니 오탐이 나왔고 전부 이 셋에 해당했습니다. **단언 셋을 함께 넣습니다** — 주석 부류 두 종류가 KINDS 표에 있고 MINOR 이고 사람을 부르지 않는지, 그리고 `comment-rot` 설명이 예외 셋을 적는지 봅니다. 행 삭제·등급 상향·예외 문구 제거 세 변이에서 각각 실패하고 되돌리면 통과하는 것을 확인했습니다. 이 종류가 나온 계기는 다른 저장소(`agent`)에 같은 규칙을 넣으면서였고, 그쪽 규칙 문안과 예외 셋이 같은 뜻이어야 합니다. **함께 `rubric.base.md` 의 비유 표현 넷을 걷었습니다** — "회차를 태운다" 셋과 "아무것도 잠그지 않음" 하나입니다. 새 행이 이웃 행과 같은 개념을 말하는데 한쪽만 비유로 적혀 있으면, 그 문장을 설명으로 옮기는 사람이 정의 없이 그 말만 가져갑니다. 이 문서는 렌즈가 런타임에 읽는 권위 문서라 특히 그렇습니다.
- **1.4.1** — **1.4.0 을 세 축으로 리뷰해 나온 것을 고칩니다. 절반이 **되돌려도 검사가 통과하던 자리**였습니다.** 서로 모르는 리뷰어 셋(단순성·영향 범위·호환성)에게 같은 diff 를 주고, 지적을 실측으로 다시 재서 채택했습니다. 가장 무거운 둘은 변이로 재현됐습니다. **하나, codex 트리의 `non_goals`·`in_scope` 를 열네 줄 전부 딴 이름으로 바꿔도 시험 8건이 통과했습니다.** 그 트리에는 결정론 게이트가 없어 산문이 계약의 전부인데(그 시험의 독스트링이 스스로 그렇게 적습니다) 필드 목록이 셋에 멈춰 있었습니다. **둘, `/build` 의 런타임 배선 두 줄**(`non_goals` 를 창에 내는 줄, `out_of_scope` 요약 줄)**을 지워도 188건이 통과했습니다.** 앞엣것이 없으면 렌즈에 넘길 값이 없어 계측이 통째로 미표시가 되고, 뒤엣것이 없으면 그 수치가 사람 눈에 한 번도 안 닿습니다. 같은 블록에 컨벤션 문서 값을 확인하는 단언이 이미 있었는데 그 줄만 못 받았습니다. 둘 다 검사를 붙였고, 되돌리면 그 검사가 실패하는 것을 확인했습니다(한쪽만 고쳐 계약이 갈라지는 경우도 잡힙니다). **계약이 스스로 모순되던 자리 넷도 고칩니다.** 계측의 기둥은 "`non_goals` 를 못 받은 렌즈는 `in_scope` 를 생략한다" 인데, checker 정의의 절대 원칙과 출력 예시 둘, codex 쪽 출력 형식 정본이 조건 없이 그 필드를 요구하고 있었습니다. 렌즈가 앞줄을 따르면 안 잰 회차가 "쟀는데 범위 밖 없음" 으로 둔갑합니다. `rubric.base.md` 가 "이 다섯만 태깅한다" 로 남아 있던 것도 함께 고쳤습니다 — 렌즈가 런타임에 읽는 권위 문서입니다. **그리고 1.4.0 항목에 업그레이드 안내를 소급해 넣었습니다.** 진행 중이던 루프가 재개에서 막히는데 그 문단이 빠져 있었고, 같은 성격의 변화에서 0.9.12·1.0.0 은 그것을 적었습니다. 재개가 막혔을 때 나오던 "Step 1 로 돌아가라" 는 메시지도 고쳤습니다 — Step 1 은 분해를 새로 쓰는 단계라 그대로 따르면 `status=done` 진행도가 날아가고, 재개 절은 정반대를 지시하고 있었습니다. 그 밖에 `/spec` 의 "안 만드는 것" 절은 문서에 하나인데 `non_goals` 는 phase 마다 하나라는 **알갱이 차이**를 양 트리에 적었고(그대로 복사하면 phase 별 좁히기가 안 생깁니다. `/spec` 을 안 거치고 설계 문서로 바로 `/build` 를 부르는 경로가 있어 `/build` 쪽에도 같이 적었습니다), codex brake 절에 `out_of_scope` 를 사람에게 올리라는 지시를 더했고, 시험 픽스처를 채우던 호출자 하나뿐인 함수를 지우고 리터럴에 직접 적었습니다. 단순성 리뷰가 낸 나머지 하나(착수 전 검사 jq 가 스킬 문서 두 블록에 복제된 것)는 이번 변경 이전부터 있던 구조라 따로 둡니다 — 엔진 스크립트로 내리면 그 검사가 엔진 시험의 직접 사정권에 들어옵니다.
  **같은 방식으로 한 번 더 리뷰해서 나온 것을 이 릴리스에 함께 넣었습니다. 이번에도 절반이 같은 자리였습니다.** 앞 회차가 값을 넘기는 두 줄에는 검사를 붙였는데 **정작 이 릴리스가 고친 계약 문구 쪽에는 단언이 하나도 없었습니다.** 그 사실을 이 릴리스 자신이 증명했습니다 — checker 정의의 필드 열거를 조건부로 고치면서 **같은 파일 앞머리 `description` 의 사본을 놓쳤고**, 검사 넷이 전부 통과했습니다. 계약 문구를 담은 열 파일을 옛 판으로 되돌려도 넷이 통과한다는 것이 실측됐습니다. 그래서 단언 다섯을 더합니다. 앞머리가 출력 필드를 다시 열거하면 실패하고(열거를 지워 사본 자체를 없앴습니다), 렌즈가 런타임에 읽는 `rubric.base.md` 는 `in_scope` 를 적을 때 조건도 함께 적어야 하고(조건만 빠져도 필드가 통째로 빠져도 실패합니다), codex 역할 계약은 조건절 두 방향을 다 적어야 하고, 이름 대조는 단어 경계로 봅니다(부분 문자열 대조는 `in_scopeZZ` 같은 접미사 개명을 그대로 통과시켰습니다), codex 착수 게이트 대조는 파일 전체가 아니라 **그 게이트 항목만** 봅니다(전체를 보면 게이트 문장에서 요구가 사라져도 다른 절의 언급이 대신 맞습니다). 다섯 다 되돌려서 실패하는 것을 확인했습니다. **그리고 앞 회차가 진단 문구를 값으로 내보내던 것을 고쳤습니다.** phase 를 못 찾았을 때 그 문구가 렌즈 프롬프트에 그대로 옮겨져 "안 볼 표면" 의 이름이 되고, 그 표면을 구현했다고 판정하면 BLOCKER 까지 갑니다. 종료코드도 0 이라 그대로 계속 갔습니다. 이제 멈춥니다 — 빈 `PHASE` 와 이름이 겹치는 phase 도 같은 자리에서 막습니다(뒤엣것의 답이 조용히 사라지던 입력입니다). 재개가 막혔을 때 나오는 메시지도 **누가** 채우는지를 적습니다. 그 자리를 도는 것은 위임 오케스트레이터인데 `non_goals` 를 적는 것은 phase 목표를 좁히는 일이라 그 층이 사람에게 올려야 하는 종류입니다. 트러블슈팅 표에 그 메시지 행과 phase 조회 실패 행을 더했고, codex 트리에도 "분해를 새로 쓰지 마라" 를 넣었습니다(그 트리에는 셸 게이트가 없어 산문이 빠지면 대신 잡는 것이 없습니다).
  **업그레이드 시 확인할 것 하나** — 이 버전으로 올린 뒤 `/review` 나 `/build` 를 돌리기 전에 **세션을 새로 시작하십시오.** 스킬 문서는 클론의 현재 브랜치에서 읽히는데 에이전트 정의는 세션 시작 시점에 고정되므로, 브랜치만 올린 창에서는 새 스킬과 옛 checker 정의가 함께 돕니다. 그 창에서 `/review` 는 `in_scope` 가 전부 미표시로 나온다고 적지만 옛 정의를 든 렌즈는 조건 없이 그 필드를 달아, 안 잰 축이 "쟀는데 범위 밖 없음" 으로 집계됩니다. 멈추지 않고 틀린 숫자를 내는 쪽이라 1.4.0 의 `exit 65` 보다 조용합니다.
- **1.4.0** — **phase 마다 "이번에 안 볼 표면" 을 적게 하고, 범위 밖 지적이 몇 건인지 셉니다.** 무인 루프가 회차 상한까지 가서야 닫히는 자리를 실측했습니다. `agent-ts` 저장 계층 루프의 `test-harness-isolation` phase 는 **완료 조건 여섯이 되돌림으로 전부 성립했는데 조건 밖 지적만으로 일곱 회차를 썼고**, CRITICAL 추이가 3→2→5→2→4→5 이면서 같은 자리 반복은 0이었습니다 — 하나를 고치면 checker 가 매번 다른 자리를 찾았다는 뜻입니다. 원인은 finding 에 **심각도 축만 있고 "이번 phase 의 범위인가" 라는 축이 없는 것**이었습니다. 그래서 `phases.json` 에 phase 별 `non_goals` 를 필수 자리로 더합니다. 판정은 `irreversible` 과 같은 모양입니다 — 안 좁히면 `false`, 좁히면 표면을 적은 비지 않은 문자열 배열이고, `true` 는 "좁히는데 어딘지 안 적음" 이라 거부합니다. 이 칸이 여는 것이 둘입니다. **하나는 바로 섭니다** — 문서가 "안 한다" 고 적은 것을 구현하면 `intent-nongoal-violation`(BLOCKER)이 뜹니다. 그 종류는 계약에 원래 있었는데 **적을 칸이 없어 아무도 그 기준을 받은 적이 없었습니다.** 다른 하나는 계측입니다 — checker 렌즈가 finding 마다 `in_scope` 를 달고 `decide.sh` 가 `out_of_scope` 로 셉니다. **그 수는 등급을 안 바꿉니다.** verdict 는 여전히 `BLOCKER 0 AND CRITICAL 0` 이고, 범위 밖이라고 강등하지 않습니다 — 근거가 아직 한 저장소 한 루프뿐이라 **재는 장치를 먼저 두고** 수치가 쌓인 뒤에 강등을 얹을지 정합니다. 등급을 내리는 축은 이 엔진에 없던 방향이고, 하필 `.loop/rubric.md` 가 채점받는 쪽도 쓸 수 있는 자리라 방어를 먼저 갖춰야 합니다. 세는 방식에 조건이 둘 붙습니다. **`in_scope` 는 세 상태입니다**(범위 안 / 범위 밖 / 안 달림) — 두 렌즈가 같은 자리를 접을 때 참거짓으로 접으면 안 단 것이 "범위 밖" 으로 떨어져 계측이 거짓 수치를 냅니다. 그리고 **`false` 라고 분명히 말한 것만** 범위 밖으로 셉니다. `"false"` 같은 오타 값은 미표시로 가는데, 오타를 범위 밖으로 읽으면 나중에 강등을 얹었을 때 그 오타 하나가 등급을 내립니다. 세 갈래의 합이 항상 총 finding 수와 같아, **미표시가 총수와 같은 회차**는 "범위 밖이 없다" 가 아니라 "렌즈가 아무것도 표시하지 않았다" 로 읽힙니다. `out_of_scope` 는 회차 요약에 한 줄로 나오고, 회차를 다 쓰고 멈춘 자리에서 `exit_criteria_probes` 와 함께 사람에게 올라갑니다 — 조건이 성립했나와 남은 지적이 이번 목표 안인가는 같은 판단의 두 반쪽입니다. 그 판단 자체는 여전히 사람이 합니다. 달라진 것은 **재료가 사람 머릿속이 아니라 파일에 있다**는 것뿐입니다. **업그레이드 시 확인할 것 하나** — 진행 중이던 `/build` 가 있으면 재개 전에 그 `phases.json` 의 phase 마다 `non_goals` 를 채우십시오(안 좁혔으면 `false`). 1.3.x 에서 만든 분해에는 그 자리가 없어 순회 입구에서 `exit 65` 로 멈춥니다. **그때 Step 1 로 돌아가지 마십시오** — 그 자리만 채우고 재개하면 됩니다(Step 1 은 분해를 새로 쓰는 단계라 `status=done` 진행도가 날아갑니다). 반대 방향은 안전합니다. 1.4.0 이 쓴 파일은 1.3.x 엔진에서 남는 키로 무시되므로 되돌리기가 데이터 때문에 막히지 않습니다. 엔진 단언 169 → 180.
- **1.3.1** — **설계 문서 포인터가 접두어 없는 파일명을 찾습니다.** `render_design_pointer_block` 이 `docs/design/domain_{name}.md` 를 찾고 있었는데 대상 레포의 규약은 접두어 없는 `{name}.md` 라 존재 검사가 항상 실패했습니다. 실패하면 빈 문자열을 돌려주게 설계돼 있어 설계 문서가 있는 도메인의 모듈 `CLAUDE.md` 에 포인터가 아예 붙지 않았고, 에러가 아니라 누락으로 끝나 눈에 띄지 않았습니다. 이 함수에는 테스트가 없어 그대로 지나갔습니다 — 재발 방지 테스트 4개를 넣었습니다.
- **1.3.0** — **문서가 뒤따라오는 것과 코드가 문서를 어기는 것을 가릅니다.** 무인 루프가 회차를 태우는 원인을 실측했습니다. `agent-ts` 저장 계층 phase 여덟 회차에서 통과를 막은 72건의 1위가 `intent-requirement-missing` 21건(29%)이고 **그중 13건이 문서 위치**였습니다. 코드는 정상인데 문서가 아직 못 따라온 자리가 코드 결함과 같은 등급을 받아 매번 다음 회차로 돌아갔습니다. 종전에는 두 방향이 한 종류에 묶여 있었으니 등급이 갈릴 방법이 없었습니다.

  **BASE rubric 에 `doc-lags-code`(MINOR)를 더했습니다.** 코드의 새 결정·필드·DDL 이 문서에 아직 없거나, 문서가 적은 개수·목록·경로가 코드보다 낡은 자리입니다. `intent` floor(MAJOR) 아래라 잡히기는 하되 통과를 막지 않습니다(`comment-noise` 와 같은 처리). **코드가 문서를 어기는 반대 방향(`intent-requirement-missing`·`intent-nongoal-violation`)은 등급 그대로입니다** — 그건 진짜 결함이라 회차를 태우는 것이 맞습니다. `loop-checker` 의 intent 렌즈도 방향으로 종류를 가르게 고쳤습니다. 무엇을 고쳐야 하는지가 다르기 때문입니다. 한쪽은 코드를, 다른 쪽은 문서만 고치면 됩니다.

  **그리고 완료 조건 검증을 출력 계약으로 세웁니다.** phase 의 완료 조건 성립 여부는 채점에 안 들어갑니다(PASS 는 `BLOCKER 0 AND CRITICAL 0` 이고 완료 조건을 안 봅니다). 그런데 **사람이 그 phase 를 닫을지 정할 때는 그것이 유일한 근거입니다.** `exit_criteria` 를 프롬프트로 받은 checker 는 이제 `exit_criteria_probes` 를 함께 냅니다 — 조건마다 무엇을 되돌렸고 무엇으로 쟀고 무엇이 빨개졌는지를 적습니다. **산문으로 판정하지 않고 되돌려서 잽니다.** 되돌리기는 작업 트리 사본에서 하고 원본이 안 변한 것을 확인해 그 사실도 적습니다. 같은 저장소에서 "완료 조건 열 개 전부 성립" 이라는 보고의 근거를 결과 파일에서 못 찾아 그 보고를 믿고 phase 를 닫을 수 없었던 일이 있었고, 다음 회차에 요구해서야 기록이 생겼습니다. **요구해야 생기는 것은 계약이 아닙니다.**

- **1.2.1** — **안티패턴 씨앗 생성기에도 덮어쓰기 가드를 답니다.** 문서를 쓰는 스크립트는 사람이 인수한 문서(자동 생성 서명이 없는 파일)를 덮지 않고 `exit 3` 으로 거부하는데, `extract_antipatterns.py` 만 그 가드가 빠져 있었습니다. **하필 산출물이 "초안이고 사람이 골라 옮기는 것" 인 유일한 스크립트입니다.** 관례상 `--out` 은 `.ai-ready/scaffolds/` 를 가리키지만 관례는 장치가 아니라, 누가 채택된 `docs/ANTIPATTERNS.md` 를 넘기면 손으로 추린 항목이 git 히스토리 덤프로 조용히 덮입니다.

  **가드를 다는 것과 서명을 박는 것은 한 쌍입니다.** 씨앗 헤더에 자동 생성 서명이 없어서, 가드만 달면 **두 번째 감사가 자기가 만든 씨앗에 막혀 죽습니다**(1회차에 만든 파일을 2회차가 못 덮음). 헤더에 서명 한 줄을 넣어 재생성은 그대로 되게 했습니다. 그 줄을 지우면 이후 실행이 그 파일을 안 덮는다는 것도 줄 안에 적었습니다 — 사람이 그 씨앗을 그대로 인수하고 싶을 때 쓰는 손잡이입니다. 시험 184(claude, 신규 5), 8(codex).

- **1.2.0** — **단일 모듈 저장소의 "논리 모듈이 어디서 시작하나" 를 스택 어댑터가 답합니다.** 종전에는 그 답이 JVM 으로만 하드코딩돼 있었고, 그것도 `audit.py` 와 `scaffold.py` 에 **두 벌로 복사**돼 있었습니다. 스택을 하나 늘리려면 두 곳을 고쳐야 했고 한쪽만 고치면 채점과 생성이 서로 다른 디렉토리를 봅니다. 새 `skills/audit/scripts/stacks.py` 의 `ADAPTERS` 하나가 단일 출처가 되고, 둘이 그것을 부릅니다. 지금 실린 것은 `jvm`·`node`·`python`·`go`·`rust` 다섯이며, **스택을 더하는 비용은 등록 한 줄**입니다 — 어댑터는 "논리 모듈의 부모 디렉토리가 어디인가" 하나만 답하고 호출부는 목록을 훑으므로 고칠 곳이 없습니다.

  **어댑터가 없는 스택에서 조용히 끝나지 않습니다.** 종전 `scaffold.py` 는 JVM 이 아니면 "수동으로 작성하세요" 한 줄을 찍고 **종료코드 0** 으로 끝났습니다. 산출물 0개인 실행과 성공한 실행이 호출한 쪽에서 똑같아 보였고, 안내문은 사람만 읽지 스크립트는 못 읽습니다. 이제 어댑터가 없으면 `3`, 기준점은 찾았는데 그 아래 코드가 없으면 `4` 로 끝나며, 무엇을 봤고(루트 매니페스트) 어디에 어댑터를 더하면 되는지까지 말합니다.

  **JVM 어댑터의 원래 결함 둘을 함께 고쳤습니다.** 마커를 정확히 `Application.kt` 로만 봐서 스프링 관례인 `FooApplication.kt` 를 놓쳤습니다 — 실제 프로젝트 대부분이 그 형태라, JVM 프로젝트인데도 JVM 으로 안 잡히는 상태였습니다. 이제 접두를 허용합니다. 그리고 Application 클래스가 아예 없는 **라이브러리 모듈**은 패키지 사슬을 내려가 base package 를 잡습니다. `scaffold.py` 쪽 마커 선택이 `rglob` 첫 매칭이라 실행마다 base package 가 달라질 수 있던 것도 `audit.py` 와 같은 결정론 규칙(경로가 가장 짧은 것)으로 맞췄습니다.

  **표준 레이아웃 조언이 스택을 가립니다.** `controller/service/domain/repository` 는 JVM 웹 관례인데, 그 규칙이 0점일 때 붙는 조언이 스택과 무관하게 "그 구조로 정렬하세요" 였습니다. Node·Go 저장소에 스프링 구조를 권하는 셈입니다. 이제 그 스택에서는 **측정하지 않는다**고 이름을 붙여 말하고 카탈로그 하나로 만점이 되며, ROI 액션 문구도 같은 말을 합니다.

  **릴리스 검사가 자기 변이 픽스처를 위반으로 잡던 것을 고쳤습니다.** 1.1.0 이 넣은 중괄호 검사가 `tests/mutate_skill_blocks.py` 안의 **일부러 만든 변이체**를 실제 위반으로 세어 `build/drift-test.sh` 가 main 에서 이미 빨간 상태였습니다. 그 줄에 예외 표시(`brace-check-example`)를 달았습니다. 시험 176(claude, 신규 18), 8(codex).

- **1.1.0** — **도출층 `/spec` 이 생겼습니다.** `build` 는 스펙이 이미 있다고 보고 phase 로 쪼갭니다. 그 앞에 서서 **스펙 자체를 만드는** 층이 없었고, 착수 전 검사는 세 자리가 비면 "채우고 다시 와라" 로 돌려보낼 뿐 무엇을 채워야 하는지는 사람이 혼자 알아내야 했습니다. `/spec` 이 그 "혼자" 를 없앱니다. `loop-spec-checker` 가 결정 자리를 열거하면 넷 중 하나로 처분합니다 — 코드·컨벤션 문서에 답이 있으면 **읽은 경로와 함께** 닫고(`resolved-from-code`), 나중에 감으로 맞출 조절값은 기본값을 두고(`default`), 추측일 수밖에 없는 것만 사람에게 묻고(`asked`), "나중에" 라는 답은 미결로 남깁니다(`deferred`).

  **종료 조건이 rubric 통과가 아니라 미결 0 이고, 그 판정을 셸이 합니다.** 스펙의 구멍은 대부분 사람 머릿속에만 있어서, 작성하는 쪽에 "지적받았으니 보완해라" 를 시키면 그럴듯한 답을 채우고 다음 회차의 점검이 "이제 정해졌네" 하고 통과시킵니다. **게이트는 전부 초록인데 사람 의도와 무관한 스펙이 나오는 환각 수렴기**가 되는 자리입니다. 그래서 처분마다 요구하는 근거가 다르고(코드 출처면 경로, 기본값이면 값, 미룸이면 이유), 근거 없이 닫힌 항목은 `exit 65` 로 거부합니다. 아직 안 닫힌 것은 `exit 3` 으로 구분합니다 — "한 바퀴 더 돌아라" 와 "네가 뭔가 잘못 적었다" 가 같은 신호가 되면 안 됩니다. 라운드 상한은 3이고, 그 뒤에도 남으면 미결로 넘길지 한 번 묻습니다(답 하나가 새 결정을 여는 성질 때문에 상한이 없으면 사람이 먼저 지칩니다).

  **`loop-spec-checker` 에 다섯째 렌즈 `purpose` 가 생겼습니다.** 앞 넷(`structural`·`behavioral`·`technical`·`contract`)은 **무엇을 만들지**를 묻는데, 만들 이유가 안 정해진 자리는 그 넷 어디에도 안 걸립니다. 셋을 봅니다 — 전에 무엇을 시도했고 왜 멈췄나(모르면 같은 벽에 다시 부딪힙니다), 무엇이 되면 실패인가(`exit_criteria` 와 **다른 물음**입니다. 그건 "무엇이 되면 끝" 이고 이건 "무엇이 되면 되돌린다" 라, 이 하한이 없으면 무인 루프가 나빠지는 중에도 계속 돕니다), 안 만들 것 하나와 그 이유(범위의 바깥선이 없으면 maker 가 인접한 것을 함께 짓고 checker 가 그것을 요구되지 않은 변경으로 잡습니다). 이 렌즈만 답이 코드에 **원리적으로 없어서**, 코드에서 찾아 닫으려 하면 지어내게 된다는 것을 정의에 못박았습니다.

  **점검기를 못 부르면 대신 돌리지 않고 멈춥니다.** 대체 실행 경로를 열어 두면 그 길이 곧 기본 경로가 되고, 열거가 이 층의 전부인데 누가 했는지가 실행마다 달라지면 같은 스펙이 회차마다 다른 개수의 결정을 냅니다. 멈출 때 무엇이 없는지·왜 그런지·어떻게 고치는지를 함께 말합니다. 원장이 남아 있어 **세션을 다시 시작해도 답한 것을 다시 묻지 않습니다** — 재시작 비용이 "처음부터 다시" 면 사람은 결국 우회로를 요구합니다.

  **셸 버그 하나를 함께 고쳤습니다.** 변수 확장 바로 뒤에 한글이 붙으면 셸이 그 한글까지 변수 이름으로 읽습니다(zsh 는 빈 문자열, bash 는 깨진 바이트). 이 저장소는 셸 블록도 메시지도 한국어라 이 조합이 자연스럽게 생기고, 하필 대부분 **실패 경로의 메시지**라 평소에 안 보입니다. 사람이 그 줄을 읽는 순간은 이미 뭔가 잘못됐을 때인데 그때 숫자가 사라집니다. `build/drift-test.sh` 에 한 곳 있던 것을 고치고, 같은 패턴이 다시 들어오면 릴리스 검사가 막게 했습니다(줄 끝에 `brace-check-example` 을 단 한 줄만 예외).

  **업그레이드 시 할 일** — 플러그인을 갱신한 뒤 **Claude Code 세션을 다시 시작하십시오.** 에이전트 목록은 세션이 시작될 때 고정되므로, 돌고 있는 세션에서는 `spec` 이 의존하는 `loop-spec-checker` 를 못 부릅니다. 파일과 매니페스트가 멀쩡한데도 "Agent type not found" 가 나는 상태이고, 이 릴리스에서는 그게 오히려 기본값입니다. 시험 158(claude), 8(codex).

- **1.0.0** — **스킬 이름에서 `loop-` 접두를 떼고, 루프 스킬 둘을 하나로 합칩니다.** `loop-run`+`loop-build` → **`build`**, `loop-review` → `review`, `loop-lessons` → `lessons`(`audit`·`apply` 는 원래 접두가 없어 그대로). 둘을 합친 이유는 실질 차이가 **maker 수명 하나**뿐이었기 때문입니다 — `loop-run` 은 회차마다 새 maker 를 띄웠고 `loop-build` 는 한 phase 내내 같은 maker 를 이어갔는데, 이어가기의 명분("이 파일을 왜 이렇게 고쳤는지")은 워킹 트리와 `phases.json` 진행도로 대체되는 반면 **컨텍스트 부풀림은 대체 수단이 없고 그쪽이 더 심했습니다**(한 phase 가 여러 step 을 담고 최대 회차를 돕니다). **매 회차 새 maker 로 통일**하니 차이가 0이 됐습니다. 합쳐야 했던 더 큰 이유가 있습니다 — 0.9.12 의 착수 전 스펙 검사 셋이 `loop-build` 에만 있어서 **`/loop-run` 이 그 게이트를 통째로 우회하는 경로**였습니다. 스킬 하나가 우회로였던 셈이고, 검사 옆에 우회로가 있으면 그것은 검사가 아니라 권고입니다. 이제 변경 하나를 수렴시키는 것도 phase 가 하나인 `phases.json` 을 지나며, 그 JSON 은 사람이 쓰는 게 아니라 스킬이 대화 합의에서 옮겨 적고 사람이 승인합니다.

  **checker 가 축이 갈린 렌즈 셋으로 병렬 점검합니다.** 종전에는 한 명이 다섯 차원을 한 컨텍스트에서 순회했는데, 그러면 차원마다 쓸 탐색량이 나뉘고 먼저 선 판단에 나머지가 끌려갑니다. 이제 `contract`(compatibility+intent — 약속과 다른가) · `safety`(security+runtime — 돌 때 터지는가) · `quality`(convention+simplicity — 더 나은 형태가 있는가) 셋이 **서로를 모른 채** 자기 축만 보고 각자 자기 파일에만 씁니다. 여섯으로 쪼개지 않고 셋으로 묶은 것은 비용 때문입니다 — 매 사이클 여섯 배가 아니라 세 배에 그칩니다. 예외는 자동화 금지 영역 하나로, 비가역 다섯에 닿는 것은 자기 축이 아니어도 냅니다(놓치는 쪽이 훨씬 비싸고, 중복은 병합이 접습니다).

  **새 차원 `simplicity` 가 생겼습니다**(dimension floor `MAJOR`). "더 적은 코드로 같은 일이 되는가" 를 보는 축이 없어서, 무인 루프가 수렴시킨 코드는 과잉 설계 검사를 한 번도 안 받고 있었습니다. 추측성 추상·죽은 코드·과잉 방어·이미 있는 것의 재구현·제어 흐름 복잡도를 보되 **두 규율 아래에서만** 작동합니다: 더 단순한 등가 대안을 구체적으로 제시할 수 있을 때만 finding 을 내고, 총량이 아니라 **이번 diff 가 더한 것**만 심사합니다. floor 가 `MAJOR` 인 것은 의도입니다 — `MINOR` 면 잡아도 통과라 기록만 되고, `CRITICAL` 이면 판단이 갈리는 지적이 PASS 를 완전히 막아 취향 논쟁으로 회차를 태웁니다. `MAJOR` 는 `RETRY_SOFT` 라 고치려 시도하되 정체하면 사람이 안고 통과시킬 수 있습니다. `comment-noise` 만 예외표에서 `MINOR` 입니다(주석 문구로 회차를 태우지 않으려고).

  **병렬화가 만든 가장 큰 구멍은 개수를 세어 막습니다.** 렌즈 하나가 죽어도 남은 둘의 결과는 형식이 멀쩡해서, 세지 않으면 그 축이 **한 번도 점검되지 않은 채** 통과합니다. 새 `_loop-engine/merge_findings.sh` 가 `--expect N` 으로 렌즈 수를 못박고, 모자라거나 빈 파일이거나 형식이 깨졌거나 **렌즈마다 비교 베이스가 다르면**(서로 다른 diff 를 본 결과는 합칠 수 없습니다) `exit 65` 로 멈추며 어느 렌즈인지 이름으로 알립니다. 합칠 때 id 에 렌즈 접두를 붙여 전역 고유로 만들고(두 렌즈가 낸 `c1` 이 같은 이름이 되면 반복 표시와 maker 지시가 서로 다른 finding 을 가리킵니다), 같은 (차원·종류·위치)가 겹치면 가중은 합집합·사람 대기는 OR 로 보수적으로 접습니다. **`exit_criteria` 를 checker 프롬프트에 넘깁니다** — 착수 전 검사는 그 항목이 *있는지* 만 보고 PASS 판정은 `BLOCKER 0 AND CRITICAL 0` 이라, 그 사이에 적어 둔 완료 조건을 실제로 재는 자리가 없었습니다.

  **업그레이드 시 할 일** — 플러그인을 갱신한 뒤 호출 이름을 바꾸십시오. `/loop-run`·`/loop-build` → `/build`, `/loop-review` → `/review`, `/loop-lessons` → `/lessons`. 옛 이름은 별칭으로도 남기지 않았습니다(별칭이 다시 스펙 검사의 우회로가 됩니다). **에이전트 이름은 안 바뀝니다** — `loop-maker`·`loop-checker`·`loop-spec-checker`·`loop-lesson-synthesizer` 그대로입니다. 사람이 타이핑하지 않는 이름이고 `maker`·`checker` 같은 일반명은 다른 플러그인과 부딪히기 때문입니다. 진행 중이던 루프가 있으면 그 `.loop/run/` 상태는 버리고 다시 시작하십시오(checker 결과 파일 이름이 렌즈별로 갈렸습니다). 시험 146 → 148(스킬 블록), 150 → 169(엔진). 리뷰가 잡은 것을 함께 고쳤습니다. **가장 큰 것은 병렬화가 기존 가드를 뚫은 자리입니다** — `score.sh` 의 "findings 도 reviewed 도 비면 거부" 는 병합된 payload 하나를 보는데, 병합이 `reviewed` 를 합집합으로 접으면서 **렌즈 하나만 채우면** 그 가드가 만족됐습니다. 나머지 축은 한 번도 안 봤는데 PASS 가 났습니다(같은 입력을 단일 checker 계약으로 직결하면 exit 65 입니다). 이제 축마다 따로 묻습니다. 함께 닫은 것 넷 — 렌즈가 `base` 키를 빼면 베이스 불일치 검사에서 조용히 빠지던 것, 두 렌즈가 같은 파일을 가리켜도 개수 게이트가 통과하던 것(나중 렌즈가 앞 것을 덮어써 한 축이 사라져도 인자 수는 맞습니다), Step 2 의 스펙 검사 사본에 배열 조건이 빠져 빈 `phases` 가 공허하게 통과하던 것, 그리고 병합 셸이 인자를 공백으로 이어 붙여 **경로에 공백이 있으면 아예 못 돌던 것**입니다.

- **0.9.13** — 릴리스 이력을 README 에서 이 파일로 옮깁니다. README 의 57%(17,956자)가 이력이라, 이 플러그인이 무엇인지 알려는 사람이 설명(13,270자)보다 이력을 더 스크롤했습니다. 0.9.8 에서 매니페스트 설명문 19,805자를 잘라 README 로 옮긴 적이 있는데, 그 이력이 README 안에서 다시 17,956자로 자란 것이라 **자리를 옮긴 것으로는 안 멈춘다**는 것이 이번 근거입니다. 이력의 단일 출처는 이제 `CHANGELOG.md` 이고 드리프트 검사도 그쪽을 봅니다. 옮기면서 매니페스트 설명문 네 곳이 "Release history lives in README.md" 로 이제 없는 곳을 가리키는 것을 찾아 함께 고쳤고, 길이만 보던 검사가 그 거짓말에 초록을 주고 있었으므로 **설명문이 가리키는 이력 파일이 실재하는지** 보는 검사를 더했습니다. 플러그인 동작은 바뀌지 않았습니다.

- **0.9.12** — `loop-build` 의 **직접 모드를 없앴습니다.** 순회는 언제나 서브에이전트에 내립니다. 0.9.11 의 착수 전 스펙 검사가 실패하면 직접 모드로 도는 길이 옆에 있었는데, 검사 옆에 우회로가 있으면 그것은 검사가 아니라 권고였습니다. **`exit_criteria`·`irreversible`·`tiebreaks` 세 자리가 필수가 됩니다.** 없으면 시작하지 않고 무엇이 빠졌는지 이름으로 알립니다. 유예 기간은 없습니다. `irreversible` 은 `false` 또는 영역 이름 문자열만 받고 `true` 는 거부합니다(닿는데 어딘지 안 적은 값이라 사람에게 올릴 이름이 없습니다). 새 에이전트 **`loop-spec-checker`** 가 `/loop-run`·`/loop-build` 입구에서 그 지시가 답하지 않은 결정을 열거합니다. 경고까지고 시작을 막지 않습니다. **업그레이드 시 확인할 것 하나** — 진행 중이던 `loop-build` 가 있으면 재개 전에 그 `phases.json` 에 세 자리를 채우십시오. 0.9.11 에서 만든 분해는 이 판정을 통과한 적이 없어 재개가 멈춥니다. 시험 142 → 146.

- **0.9.11** — 같은 날 같은 저장소에서 `loop-build` 로 두 phase 를 돌렸는데 **한쪽은 여섯 사이클에 사람이 한 번 끼어들어야 닫혔고, 다른 쪽은 네 사이클에 사람이 한 번도 필요 없었습니다.** 차이는 스펙 하나뿐이었습니다 — 앞쪽은 목표가 "세웠다고 적은 장치가 실제로 잠기게 한다" 라 항목으로 나열되지 않아 하나를 닫으면 checker 가 다음을 찾았고, 뒤쪽은 목표를 **변이 여섯으로 미리 적어** 줬습니다("관성 분기를 지우면 빨개진다" 같은 것). 그래서 **사람이 끼어들어야 하는 자리는 대부분 미리 알 수 있다** 는 것을 규약으로 옮깁니다. `loop-build` 에 **위임 모드** 를 선택지로 더했습니다 — 순회를 통째로 서브에이전트에 내려 사이클 잡음(채점 결과·maker 지시·게이트 출력)이 사람이 보는 창에 안 쌓이게 하는 모드입니다. 아래층 출력은 띄운 쪽에게 가므로 그 잡음이 위로 안 올라옵니다(실측: 3,900자 출력을 그 깊이에서 냈을 때 중간 에이전트만 전문을 받았고 최상위에는 네 줄 요약만 왔습니다). **종전 방식은 그대로 둡니다** — 사람이 자주 끼어들 일이면 직접 모드가 맞고, 매번 고릅니다. 위임의 관문은 실행 중 판정이 아니라 **착수 전 스펙 검사 셋** 입니다: phase 마다 `exit_criteria`(각 항목이 "되돌리면 무엇이 빨개지나" 를 말한다), phase 마다 `irreversible`(닿는 영역을 문자열로, 안 닿으면 `false`), 최상위 `tiebreaks`(측정으로 안 갈리는 트레이드오프의 순서). 셋 중 하나라도 없으면 `exit 65` 로 멈추고 위임하지 않습니다. 이 관문은 Step 1 의 사람 승인과 **별개의 기계적 검사** 이고, 무엇이 빠졌는지 이름으로 지목합니다 — `jq -e` 한 방이면 "뭔가 빠졌다" 까지만 나와 사람이 무엇을 더 적을지 못 읽습니다. **기존 `phases.json` 은 그대로 돕니다** — 이 셋은 위임 검사에서만 요구하고 Step 2 의 순회 입력 검증은 손대지 않았습니다(호환성 대조군을 시험에 함께 뒀습니다. 둘 다 거부하면 기존 사용자가 그날로 멈춥니다). 위임 오케스트레이터가 **위로 올리는 것은 셋뿐** 입니다: 비가역 영역, 범위가 바뀌는 것(두 phase 실측에서 사람에게 올라간 유일한 종류), 그리고 설계가 미완이 아니라 **틀렸을** 때. 근거가 측정이면 올리지 않습니다 — 재서 갈리는 것은 그 층이 재고 정하고, `tiebreaks` 에 답이 있으면 그것도 안 올립니다. **그 층은 사람에게 직접 말할 수 없어** 자기 턴을 끝내고 메인에 보고하면 메인이 전달하고 답을 `SendMessage` 로 되돌립니다. 아래층 스폰에는 제약이 둘 있습니다 — 그 깊이에서는 `run_in_background` 와 `name` 을 못 써서 maker·checker 를 동기로 띄우고 `agentId` 로 잇고(한 phase 안의 RETRY 를 같은 maker 로 잇는 불변은 그대로), 그 층은 팀 명부에도 화면에도 안 보여 진행은 `.loop/run/` 의 이력 파일로 봅니다. 단언 57 → 62(스킬 블록).

- **0.9.10** — `loop-build` 로 phase 하나를 돌렸는데 **여섯 사이클이 걸렸고 그중 넷이 같은 종류**(`test-vacuous`)였습니다. 1회차는 "고친 코드가 안 잠겼다", 2회차는 "잠그려고 세운 검사가 안 잠겼다", 3회차는 "그 검사가 잠그는 것은 상수의 내용뿐이다" — 매번 한 겹씩 깊어지고 등급은 나빠졌습니다. **원인은 코드가 아니라 phase 목표였습니다.** 목표가 "세웠다고 적은 장치가 실제로 잠기게 한다" 였는데 이건 끝나는 지점이 없는 목표입니다. 하나를 잠그면 checker 가 다음 안 잠긴 것을 찾고, 사람이 범위를 하나로 좁혀 준 뒤에야 닫혔습니다. **판정부는 그동안 아무 신호도 내지 않았습니다** — `stall.sh` 는 등급 개수 벡터만 보고 finding 의 종류는 안 봐서, 같은 종류가 계속 나온다는 사실이 어디에도 안 잡히고 등급이 오르내리는 동안 `PROGRESS` 까지 떴습니다. 이제 `kindstreak.sh` 가 사이클마다 그 사이클을 지배한 종류(가장 높은 등급의 finding 들 중 최빈 kind)를 하나 정하고, 마지막 사이클부터 거슬러 같은 종류가 연속 지배한 횟수를 셉니다. `repeated_kind_cycles`(기본 3, rubric PARAMS) 이상이면 멈추고 사람을 부르되 **전할 말이 정체와 다릅니다** — "코드가 안 고쳐진다" 가 아니라 "같은 종류가 N 사이클 지배했다, 코드가 아니라 목표를 의심하라" 이고, 목표가 열거 가능한지와 끝나는 지점이 정의됐는지를 사람이 봅니다. MINOR 만 있는 사이클은 게이트를 통과시키는 등급이라 연속을 끊지도 잇지도 않고 건너뛰며, 최빈이 동점이거나 finding 이 없는 사이클은 연속을 끊습니다. 이력을 못 읽으면 `exit 65` 로 시끄럽게 죽습니다 — 조용히 `OK` 를 내면 감지기가 영영 안 울리는데 그 침묵이 통과로 보입니다. **finding 의 `severity` 가 사다리 밖이거나 없을 때도 같습니다.** 처음 판은 그런 finding 을 MINOR 와 같은 길로 보내 건너뛰었고, 등급 오타 하나(`CRITCAL`)에 같은 종류가 네 사이클 연속인 이력이 `streak: 0` 으로 나왔습니다 — 감지기가 눈이 먼 채 통과를 냈습니다. 이 감지기를 만들게 한 병(확인 못 한 것이 통과 방향으로 떨어지는 것)과 같은 종류라, `MINOR` 건너뛰기는 규칙으로 두고 사다리 밖 값은 어느 사이클의 어떤 값인지 짚어 거부합니다. `stall.sh` 는 건드리지 않았습니다(입력 계약을 바꾸는 대신 옆에 세웠습니다). `loop-build` 의 "좋은 step 의 원칙" 에는 **사전** 규율을 적었습니다 — step 마다 실행 가능한 AC 가 있어도 phase 목표가 열거 불가능하면 수렴하지 않으므로, 분해 단계에서 "무엇을 다 하면 이 phase 가 끝인가" 를 한 줄로 적을 수 있는지 확인합니다. 감지기는 그것이 새어 나갔을 때의 그물입니다. 단언 112 → 150(엔진).

- **0.9.9** — loop 계열 스킬의 셸 블록이 **없는 변수로 엔진 경로를 잡던 것**을 고칩니다. `$CLAUDE_PLUGIN_ROOT` 는 스킬 본문을 만들 때 치환되는 값이라 Bash 도구가 띄우는 자식 셸에는 존재하지 않습니다(실측: 빈 문자열). 그래서 `ENG="$CLAUDE_PLUGIN_ROOT/_loop-engine"` 은 `/_loop-engine` 이 됐고, **없는 경로가 있어 보이는 경로로 만들어지는** 종류라 뒤따르는 `loop_param` 이 빈 값을 내며 조용히 흘러갔습니다 — 루프를 걸 때마다 사람이 손으로 경로를 유도해야 했습니다. 이제 블록은 호스트가 스킬 본문 첫머리에 주입하는 "Base directory for this skill" 값을 자리표시자로 받아 엔진을 유도하고, 못 찾으면 그 자리에서 exit 65 로 죽습니다(`/loop-run`·`/loop-review`·`/loop-lessons`, 그리고 `audit`·`apply` 의 파이썬 스크립트 경로도 같은 방식). **이 결함이 오래 산 이유는 시험에 있었습니다** — 블록 시험 하네스가 `CLAUDE_PLUGIN_ROOT` 를 직접 주입해, 실제 환경에서는 안 도는 블록이 시험에서만 초록이었습니다. 그 주입을 걷어내고 대조군 셋을 더했습니다: 자리표시자를 안 붙여 넣으면 비0 으로 죽는가, 붙여 넣으면 실제로 엔진을 찾는가, 옛 표기는 이 셸에서 아무것도 못 찾는가. 마지막 하나가 주입 부활을 잠급니다. 블록 전수 검사도 더해 어느 스킬이든 그 변수를 다시 쓰면 시험이 먼저 멈춥니다.
- **0.9.8** — 플러그인 목록 화면에서 읽는 설명문을 **19,805자에서 754자로** 줄입니다. 릴리스마다 문단을 하나씩 덧붙여 온 결과 매니페스트 설명문이 변경 이력 전문이 됐고, 목록이 읽을 수 없는 상태였습니다. 이력은 전부 README 변경 이력에 있으므로 잃는 내용 없이 옮깁니다(설명문에 있던 v0.2.0~v0.9.7 은 이 목록의 부분집합입니다). 대신 설명문은 이 플러그인이 무엇인지를 정확히 말합니다 — 종전 첫 문단이 감사·적용 두 스킬만 소개하고 0.6.0 부터 절반을 차지한 loop 계열을 통째로 빠뜨리고 있었습니다. 그리고 **0.9.7 이 붙인 "설명문이 이 릴리스를 언급하나" 검사를 길이 상한(1,200자)으로 바꿉니다** — 그 검사를 통과하는 가장 쉬운 길이 문단을 하나 더 붙이는 것이라, 부풀림을 막겠다고 붙인 검사가 부풀림의 유인이었습니다. 릴리스 이력의 단일 출처는 README 이고 그쪽은 별도 검사가 지킵니다.

- **0.9.7** — 채점에서 **모델이 조용히 등급을 정하던 자리**를 표로 옮기고, "안 본 것" 을 통과로 안 읽습니다. `severity 는 checker 가 매기지 않는다` 는 참이지만 등급을 정하는 입력 셋(`kind`·`dimension`·`weights`)이 전부 checker 에서 왔습니다 — 같은 운영 DDL 삭제가 가중 표시 하나로 `RETRY` 와 `AWAIT_USER` 로 갈렸고, 사람을 부를지를 단어 하나가 정하고 있었습니다. **자동화 금지 다섯에 `kind_id` 를 줬습니다**(`ddl-safety`·`money-path-change`·`authz-policy-change`·`mass-dispatch`·`destructive-data-op`) — 산문은 "표의 `force_await=always` 열로 적용한다" 고 적었는데 그 값을 쓰는 행이 **하나도 없어**, 사람 대기가 전적으로 프롬프트 준수에 걸려 있었습니다. 이제 종류 이름만 맞으면 표가 사람을 부릅니다. **`PATHWEIGHTS` 표**가 finding 의 경로에서 가중을 유도해 checker 것과 합치고(BASE 는 DB 마이그레이션 디렉터리만 — 돈·인가 경로는 저장소마다 달라 LOCAL 몫), **`PATHEXCLUDE` 표**가 문서·픽스처·vendor 를 빼 마이그레이션 정책 문서 오타 하나가 무인 루프를 세우던 것을 막습니다. 그 표는 LOCAL 이 BASE 경로 규칙을 끄는 유일한 길이기도 합니다. `test-vacuous` 를 종류로 세웠습니다 — `test-missing` 은 테스트가 *없는* 것만 잡고, 있는데 되돌려도 초록인 것은 이름이 없어 최하 등급으로 통과했습니다.

  **계약이 바뀝니다.** checker 는 이제 `{base, reviewed, findings}` 를 냅니다. `findings` 와 `reviewed` 가 둘 다 비면 채점이 `exit 65` 로 거부합니다 — `{"findings":[]}` 는 정상 형식이라 파일 가드를 지나서 "깨끗함" 과 "베이스 브랜치가 어긋나 diff 가 통째로 빈 것" 이 구분되지 않았고, 후자는 점검 없이 통과였습니다. 옛 checker 가 깨끗한 결과를 내면 멈추는데, 조용한 통과가 아니라 **시끄러운 사람 호출**이고 checker 프롬프트가 같은 버전에 함께 나갑니다.

  **적대적 검토 넷이 이 릴리스의 절반을 만들었습니다.** 가장 나쁜 것은 새 방어가 스스로 fail-open 이던 것입니다 — `reviewed` 자리에 배열이 아닌 값이 오면 길이 계산이 jq 에서 죽고 그 실패가 빈 문자열이 되어 조건이 거짓이 되며 게이트가 통째로 열렸습니다. 그 밖에 `force_await` 를 엄격 비교해 표의 어휘인 `"always"` 를 무시하던 것(부정 목록으로 뒤집음), 관대한 kind 가 dimension floor 를 완전히 이기던 것(어긋나면 높은 쪽), 배열 아닌 `weights` 가 같은 배치의 진짜 BLOCKER 까지 날리던 것, BASE 경로 패턴이 루트 `migrations/`·Rails `db/migrate/`·Liquibase `db/changelog`·대문자 경로를 놓치던 것(줄 번호를 떼고 대소문자 무시로 맞춤), LOCAL 한 줄로 금지행 다섯이 꺼지던 것(`force_await=always` 만 합집합 병합 — 그 파일은 채점받는 maker 가 쓸 수 있는 자리입니다), 열이 모자란 KINDS 행과 허용 표 밖·빈 `PATHWEIGHTS` 키가 조용히 사라지던 것(전부 `exit 65`), `stall.sh` 상태 파일에 검증이 없어 음수 하나로 정체 감지가 영구히 죽던 것, 그리고 **오케스트레이터가 `score.sh` 의 `exit 65` 를 삼켜** verdict 가 미정의로 흐르고 회차 카운터까지 멈추던 것을 고쳤습니다. 소비자 없는 `layer` 출력과 BASE 와 중복이던 `detect_build` 의 postgres 후보는 뺐습니다. 단언 33 → 112(엔진), 50 → 52(스킬 블록 하네스).

- **0.9.6** — Claude 트리에 effort 사다리를 적용해 판정부를 세션에서 떼어냅니다. (이 항목은 0.9.7 에서 소급 기록했습니다 — 0.9.6 릴리스가 이 목록과 매니페스트 버전 셋 중 둘을 함께 올리지 않아 `build/drift-test.sh` 에 그 누락을 잡는 검사를 붙였습니다.)

- **0.9.5** — `loop-run` 의 maker 를 세션에서 떼어내 loop 계열 넷의 계약을 맞춥니다. `loop-run`·`loop-build` 를 claude·codex 두 트리로 세면 넷인데 maker 를 메인 세션이 겸하는 것은 claude `loop-run` 하나뿐이었고, 그 차이가 어느 파일에도 적혀 있지 않아 의도인지 갈라진 것인지 판정할 근거가 없었습니다. 이제 넷 다 maker 를 서브에이전트에 위임하고 남은 차이는 수명 하나입니다 — `loop-run` 은 회차마다 새로, `loop-build` 는 phase 마다 새로 띄우고 그 phase 안의 재시도만 같은 maker 를 이어갑니다. 분리의 부수 이득으로 `loop-run` 도 모델 비대칭을 쓸 수 있게 됐습니다(구현은 `loop-maker` 기본값 opus, 검증은 세션 모델을 상속하는 checker). **작업 지시는 파일이어야 합니다** — maker 가 서브에이전트라 대화 맥락을 물려받지 못하고, 회차마다 요약을 다시 만들면 5회차 지시가 1회차와 달라집니다. maker 와 checker 가 같은 파일을 받으므로 둘이 서로 다른 요약으로 일하다 정합 판정이 어긋나는 일도 없어집니다. 합의가 대화에만 있으면 오케스트레이터가 브리프 한 장을 `$LOOP_DIR/brief.md` 에 쓰고 **사람에게 확인받은 뒤** 첫 회차를 엽니다 — 무인 실행에서 사람이 거치는 유일한 관문이고, 열 회차를 돌릴 지시를 사람이 한 번도 보지 않고 떠나는 것이 원래 위태로운 자리였습니다. 회차를 건너 전달되는 것은 대화가 아닙니다. 앞 회차가 무엇을 시도했는지는 워킹 트리에 그대로 있어 새 maker 가 `git diff` 로 읽고, 같은 결함이 몇 회차째인지는 `history.jsonl` 에서 뽑는 finding 단위 반복 표시가 알려줍니다. 같은 정보를 대화로 실어 나르는 쪽이 더 비쌉니다. **게이트를 돌리기 전에 트리가 실제로 바뀠는지 확인합니다** — maker 가 고쳤다고 보고하고 아무것도 안 바꿨으면 게이트가 같은 결과를, checker 가 같은 finding 을 내고 회차만 탑니다. 변경이 없으면 회차가 아니라 정체 신호입니다. "매 회차 코드가 바뀌어야 루프가 의미 있다" 는 요구에 처음으로 집행이 붙었습니다. maker 보고는 `ok` 또는 `blocked: <한 줄 사유>` 로 줄었습니다. 분기 입력이 셸이 낸 verdict·정체 판정·brake 카운터 셋뿐이라 산문 보고가 어느 분기에도 들어가지 않았고, `blocked` 만 `AWAIT_USER` 로 분기를 바꿉니다. `blocked` 를 다른 maker 로 돌리지 않고 `loop-build` 처럼 phase 요약이 필요한 자리에서만 오케스트레이터가 더 긴 보고를 명시로 요구합니다. `loop-maker` 는 이제 두 스킬이 공유하고, 빌드 명령을 받으면 컴파일만 스스로 돌려 확인합니다(테스트는 게이트의 몫). **성능 개선이 아니라 정합 맞추기입니다** — 오케스트레이터 컨텍스트 누적은 예측이고 측정치가 아닙니다. 남아 있는 루프 흔적은 전부 `loop-build` 이고 관측된 회차가 1~3이라, 이 릴리스는 없는 수치가 아니라 넷 사이의 불일치를 근거로 삼습니다.
- **0.9.4** — 게이트 실패 출력을 버리지 않고 항목 큐로 만듭니다. 종전에는 빌드·테스트가 깨지면 그 출력 전문이 오케스트레이터 창에 그대로 쏟아지고, 다음 회차에 또 쏟아졌습니다. checker 가 낸 결함은 `scored.json` 에 구조화돼 항목 단위로 처리되는데 게이트 실패만 원문 덩어리였습니다. 이제 `loop-run` 이 게이트 출력을 파일로 받고 새 `_loop-engine/gate_parse.py` 가 한 줄 하나의 JSON 항목(`kind`·`file`·`line_number`·`message`·`raw`)으로 바꿔 `gate-queue.jsonl` 에 쌓습니다. maker 는 컴파일 오류 → 테스트 실패 → 린트 위반 순으로 꺼내 고치고, 창에는 한 줄 목록만 나갑니다. 실측으로 빌드 출력 30줄이 항목 2건, 린트 46줄이 13건, 테스트 59줄이 1건이 됐습니다. **게이트 큐가 비어 있지 않으면 `scored.json` 보다 먼저입니다** — 게이트가 깨진 사이클은 checker 를 부르지 않아 그 파일이 앞 사이클 값으로 남아 있고, 그걸 먼저 고치면 이미 없는 문제를 쫓습니다. 큐는 사이클마다 비우고 새로 채웁니다. 게이트를 매 사이클 다시 돌리므로 그 출력이 유일한 정본이고, 앞 회차 항목이 남으면 고쳐진 오류를 계속 쫓게 됩니다. **아는 형식이 하나도 없어도 큐가 비지 않습니다** — 출력 꼬리를 `gate-output-unparsed` 항목 하나로 남깁니다. 조용히 버리면 큐가 비어 게이트가 통과한 것처럼 보이고 그 오독이 이 큐가 막는 실패입니다. 형식은 기억이 아니라 실제 출력에서 떴습니다. Kotlin 2.x 는 **열 번호 뒤에 콜론이 없어**(`...:17:31 Unresolved reference`) 기억대로 쓴 정규식은 한 줄도 못 맞혔을 것이고, 정규식과 테스트 픽스처를 같은 기억으로 쓰면 둘 다 초록인 채 실전에서 0건이 됩니다. 구버전의 콜론 형식도 함께 받습니다. 형식 회귀는 `test_gate_parse.py` 24건이 잡고 `test.sh` 에 붙어 있습니다.
- **0.9.3** — 금지 가이드 만점에 구체성을 요구합니다. 금지 줄을 줄 수로만 세면 "금지" 라는 단어를 세 번 적는 것이 최적 전략이 됩니다. 2회차 적대 검토가 실증한 키워드 농사입니다. 이제 만점은 백틱 코드 참조·경로·CamelCase 나 snake_case 식별자 같은 레포 특정 지시어를 담은 줄 3개를 요구하고, 막연한 금지 줄만 있으면 부분점수 3점까지만 받습니다. 식별자 없는 정당한 규칙이 0점으로 떨어지지 않게 하기 위한 완충입니다. 반입 전 실측으로 agent 는 92줄 중 50줄, c8c-api 는 378줄 중 242줄이 구체성 기준을 통과해 두 레포 모두 만점 기준 3줄을 크게 웃돌았고 실제 점수는 규칙 단위까지 변동이 없었습니다. 탈락분은 섹션 제목과 막연한 산문뿐입니다. 사용 시점 규칙에는 같은 계단을 두지 않습니다 — 문서당 가이드 하나가 정상이라 반복 표기를 유인하기 때문입니다.
- **0.9.2** — 같은 2회차 검토의 문서 정합 묶음입니다. (1) 생성되는 `.ai-ready/README.md` 의 산출물 표가 산출 디렉터리의 실물 기준이 됩니다 — audit 은 `dashboard.html` 과 `scaffolds/` 를 만들지 않는데(각각 dashboard.py 와 apply 의 몫) 종전 README 가 무조건 표에 넣고 "매 실행 시 갱신"이라 적어, audit 만 재실행한 독자가 낡은 dashboard 점수를 갱신된 것으로 믿게 했습니다. 없는 산출물은 표 대신 생성 방법 안내로 바뀌고, dashboard 는 dashboard.py 를 다시 돌려야 갱신된다고 명시합니다. (2) apply 의 루트 문서 초안 지시가 바이트 기준(800~8,000)으로 바뀝니다 — 채점이 0.8.9 부터 바이트인데 지시만 "50~150줄"로 남아, 0.8.9 의 근거 사례(46줄 = 12,029바이트)처럼 줄 수로는 통과해도 바이트로 초과할 수 있었습니다. (3) MOC 용어를 배점표 첫 등장 자리에서 풀이하고(Map of Content), apply 매핑 표의 닫히지 않는 볼드 마커를 고치고, 0.9.1 채점 노트가 실제 구현보다 넓게 말하던 "hook/CI 명령 실재 확인"을 settings 훅 한정으로 바로잡습니다(CI 는 실행 디렉터리를 알 수 없어 판정 제외).
- **0.9.1** — 2회차 적대 검토(다른 모델의 새 시선)가 실측으로 확정한 우회로들을 막습니다. (1) 루트 문서 경로 참조는 실재하고 스텁이 아닌 대상만 셉니다 — 존재하지 않는 파일을 가리키는 링크 3개로 4/4 를 받았습니다. 깨진 참조는 비고에 나열돼 링크 부패 감지도 겸합니다. (2) 이진 파일이 최소 내용 게이트를 넘지 못합니다(앞 8KB NUL 검사) — 스텁 ADR 옆 4KB 바이너리 하나로 ADR 규칙이 만점이 됐습니다. 디렉터리 게이트는 텍스트 문서 확장자만 셉니다. (3) 갱신 프로토콜 키워드는 실질 문서에 있어야 합니다 — 3줄 스텁의 "갱신 트리거" 한 줄로 5/5 였고, 0.9.0 이 금지·사용 시점 두 형제 규칙만 게이트한 비대칭이었습니다. (4) 훅 명령이 가리키는 레포 상대 스크립트가 없으면 부분점수입니다 — 실재하지 않는 `./gradlew` 와 `freshness_check.py` 를 가리키는 훅 문자열이 검증 훅·신선도 훅 만점을 받았습니다. PATH 명령은 실행 환경을 모르므로 판정하지 않고, 같은 이유로 CI yml 은 이번 판정 대상에서 뺐습니다. (5) config `build_deps` 는 4자 이상이면서 의존성 선언 줄에서만 인정합니다 — 빈 매니페스트의 `plugins {}` 에 "plugins" 를 선언해 2→5 가 됐습니다. config 로 인정된 점수는 리포트 머리에 "config 자기신고 인정" 으로 합산 공개됩니다. 채점당하는 레포가 쓰는 파일이 채점 입력이라는 구조는 유지하되 그 비중을 드러냅니다. (6) antipatterns `doc_hints` 가 스텁 ANTIPATTERNS.md 의 부분점수에 막히지 않고 도달합니다 — naming 힌트와 같은 의미론으로 맞춰, 통합 문서로 관리하는 레포가 낡은 스텁을 지워야만 인정받던 비대칭을 없앱니다. **채점값이 달라져 추이 재기준이 필요합니다** — 실측으로 껍데기 레포 52 → 38(AI 미인지), config 공격 상한 63 → 46(등급 상승 없음), 실제 레포 두 곳은 53 → 53, 71 → 71 로 규칙 단위까지 변동이 없었습니다.
- **0.9.0** — 껍데기 문서로 만점을 받던 구멍을 막습니다. 적대 검토 실측에서 세 줄짜리 스텁 스물한 개로 채운 레포가 100/100 "에이전트 자율" 을 받았습니다. 원인은 둘입니다. 최소 내용 게이트가 비공백 3줄로 느슨했고, 여러 규칙이 그 게이트조차 거치지 않고 파일 존재나 정규식 매칭만 봤습니다. 게이트를 **400바이트 + 비공백 8줄 동시 충족** 으로 올렸습니다. 두 조건을 함께 요구하는 것은 한쪽만 보면 아주 긴 한 줄이나 한 글자짜리 줄 여럿으로 우회되기 때문입니다. 적용 범위도 넓혀 모듈 CLAUDE.md 커버리지, 인덱스, ADR 디렉터리, API 계약 파일, 성과 지표 산출물, 그리고 금지·사용 시점 두 규칙이 새로 게이트를 거칩니다. 금지 가이드는 줄 수로 계단화해 세 줄 이상이라야 만점입니다. 상한만 있고 하한이 없던 두 규칙에 하한을 붙였습니다. 루트 문서는 800바이트 미만이면, 모듈 문서는 평균 10줄 미만이면 부분점수입니다. 종전에는 0바이트 루트 문서와 세 줄짜리 모듈 문서가 만점이었습니다. 규칙을 가리키던 번호(`Rule 2.4` 같은)를 전부 이름으로 바꿨습니다. 배점표에 번호 컬럼이 없어 정의된 좌표계가 없었고 `Rule 1.5` 는 이미 존재하지 않는 번호였습니다. **채점값이 달라져 추이 재기준이 필요합니다** — 실측으로 껍데기 레포는 100 → 52 로 내려갔고, 실제 레포 두 곳은 53 → 53, 71 → 71 로 변동이 없었습니다. 스텁에만 손해가 가는 것이 의도한 비대칭입니다.
- **0.8.9** — 루트 문서 채점 기준을 줄 수에서 **바이트**로 바꿉니다(8,000 이하 만점, 12,000 이하 부분점수). 한국어 마크다운은 한 줄이 표 한 행이거나 문단 하나라 줄 수가 비용을 대변하지 못합니다 — 실측으로 46줄짜리 루트 문서가 12,029바이트(한 불릿만 2,002자)인데 옛 200줄 규칙에서 만점을 받으며 계속 부풀었습니다. 바이트는 한글 3 / 영문 1 이라 토큰 비중에 더 가깝습니다. 근거 문자열에 바이트와 줄 수를 함께 남기고(0점일 때도 남겨) history 에서 추이가 보이게 했습니다. **채점값이 달라져 추이 재기준이 필요합니다** — 옛 규칙에서 만점이던 레포가 낮아지는 것이 의도한 교정입니다(실측 두 레포: 58→53, 74→71). 더불어 `inject_lazy_load_index` 가 "파일명이 곧 트리거" 인 문서(COMMANDS·NAMING·TESTING·ARCHITECTURE·ANTIPATTERNS·INDEX)를 자동 표에서 빼 링크 한 줄로 묶습니다. 0.8.7 이 지운 것은 *중복* 행뿐이라, 중복이 아니면서 디렉터리 목록이 이미 말해 주는 것 이상을 담지 않은 행은 남아 있었습니다. 링크는 묶음 줄에 유지되므로 규칙 1.2 의 경로 참조 수는 줄지 않습니다. 그 규칙은 참조 3건에서 포화해 그 이상은 점수를 주지 않으므로, 긴 트리거 표는 점수를 벌지 못하면서 매 세션 컨텍스트만 씁니다(RUBRIC 채점 노트에 명시). `--json` 사실에 `self_evident` 가 실려 AI 유지보수 경로도 같은 기준을 씁니다.
- **0.8.8** — 루트 문서 `## 모듈 맵` stub 의 모듈 나열 분량을 config 로 정합니다(`module_map.root_stub_limit`, 기본 10 유지). 0 으로 두면 카탈로그 링크만 남고 발췌 나열이 빠집니다. 루트 문서는 매 세션 always-loaded 이고 모듈 한 줄 요약은 그 모듈 CLAUDE.md 에 이미 있어(그 모듈을 열 때 자동 로드) 경로 참조가 다른 데서 충분한 레포에는 발췌가 중복입니다(한 레포에서 약 1,800자). 다만 audit 의 "루트 문서가 3개 이상의 모듈 경로/문서 참조" 규칙이 그 나열을 참조원으로 쓰는 레포도 있어 기본값은 그대로 두고, 0 으로 끄면 점수가 조용히 깎이지 않도록 안내를 출력합니다. 잘못된 값(음수·bool·비정수)은 생성을 멈추지 않고 기본값으로 fallback 합니다.
- **0.8.7** — `inject_lazy_load_index` 가 수동 영역이 이미 가리키는 문서를 자동 표에서 뺍니다. 루트 CLAUDE.md 는 매 세션 always-loaded 라, 같은 문서를 수동 표와 자동 표가 각각 가리키면 그 중복분이 세션마다 컨텍스트를 먹습니다(한 레포에서 12행·약 1,558자 이중 등재). 링크 대상을 후행 슬래시 정규화해 비교하고, 한 행의 대상이 전부 수동 영역에 있을 때만 뺍니다. 감지된 문서가 모두 겹치면 빈 표 대신 안내 문장을 넣습니다. 마커 없는 기존 표를 수동 영역으로 흡수하는 마이그레이션 경로에도 같이 적용돼 흡수 직후 중복이 생기지 않습니다. 지금까지 `config.override_hardcoded` 로 손수 지정해야 했던 일을 자동화한 것입니다.
- **0.8.6** — loop-build 의 phase 통과(PASS) 분기에서 maker 종료를 명시적 절차로 만듭니다. 오케스트레이터가 SendMessage 로 "phase 완료 — 종료" 통지를 보내고 응답을 기다리지 않으며, 그 maker 에는 다시 진입하지 않습니다. loop-maker 행동 규칙에도 통지를 받으면 새 작업 없이 턴을 끝내는 조항이 실립니다. 통과한 phase 의 maker 가 대기 팀메이트로 남던 것을 막습니다.
- **0.8.5** — loop-build 의 maker 를 전용 `loop-maker` 에이전트로 실체화. 행동 규칙(이 phase 만·테스트 동반·설계 결함 시 보고·5줄 보고·커밋 금지)이 스핀 프롬프트에서 에이전트 정의로 옮겨가고, frontmatter `model: opus` 가 기본값으로 실립니다 — 구현은 생산 작업이라 세션 모델 아래 급을 기본으로 두고, 검증은 세션 모델을 상속하는 checker 가 위에 서는 비대칭이 강등의 전제입니다. 호출 시 model 파라미터가 frontmatter 를 이기므로 phase 난도에 따라 상향·하향할 수 있습니다.
- **0.8.4** — loop-checker·loop-lesson-synthesizer 의 frontmatter 모델 고정(opus)을 해제. 판단 에이전트는 호출 세션의 모델을 상속하는 것이 기본이고, 특정 모델이 필요하면 Agent 호출의 model 파라미터로 지정합니다. 메인 모델 세대 교체 때 판단 에이전트가 의도치 않게 강등되는 사고를 막습니다.
- **0.8.3** — 배선·컨텍스트 위생 묶음. (1) freshness Stop 훅이 설치 직후 `CLAUDE_PLUGIN_ROOT` 부재로 죽던 버그 수리 — 훅 셸이 자기 위치에서 파이썬 스크립트를 찾고, audit 가 `.py` 도 함께 복사합니다. (2) checker·synthesizer 에게 컨벤션 문서·지식층·LOCAL rubric 경로를 **프롬프트로** 전달하도록 배선 — 환경변수는 서브에이전트에 전달되지 않아 기존 배선이 끊겨 있었습니다. (3) loop-run 이 brake 값·감지 명령을 `params.env` 로 파일 영속하고 매 Step 이 재유도 — Bash 호출 간 변수 소실로 brake 가 무력화되던 구멍을 막습니다. 재유도 포인터는 브랜치별(`.active-{브랜치}`)이라 한 체크아웃에서 멈춘 루프와 새 루프가 서로 상태를 침범하지 않고, source 는 `set -a` 로 export 까지 복원해 LOCAL rubric 이 채점 자식 프로세스에 전달됩니다. 게이트 실패 재진입도 자기완결 카운터로 brake 가 셉니다. (4) loop-build 의 stall 상태·게이트 카운터를 phase 별 파일로 분리하고, 문서로만 있던 phase 별 history 분리를 `params.env` 재정의로 실제 배선(앞 phase 잔재로 인한 거짓 STALLED 차단). 시간 상한 재계산은 phase 당 원값을 따로 영속해 재개 재실행에도 멱등합니다. (5) 오케스트레이터 컨텍스트 위생 규칙 명문화 — findings 전문을 읽지 않고 채점 요약만 보유, maker 재진입은 scored 파일 경로 인계, phases.json 갱신은 jq, 설계 세션과 오케스트레이션 세션 분리 권고. (6) decide/stall 입력 계약 fail-loud 검증, npm 기본 test 스텁 미채택, loop-review 베이스 브랜치 감지 순서·findings 경로 격리, BASE rubric 의 c8c-api 특화 security 좁히기를 LOCAL 결정으로 명시, 문서 드리프트 다수 정정.
- **0.8.2** — checker findings 를 오케스트레이터가 지정한 결정적 파일로 회수합니다. 백그라운드 세션에선 서브에이전트의 최종 메시지가 인라인으로 전달되지 않아, 파일이 유일한 회수 경로입니다. checker 의 쓰기 금지 규칙에 그 단일 경로 예외를 두고, 스핀 전 파일 비우기와 빈 파일 fail-loud 가드로 stale 결과·미기입이 통과로 둔갑하는 것을 막습니다.
- **0.8.1** — loop-build 견고성. 무인 순회를 시작·재개하기 직전 `phases.json` 을 검증해, 손상된 분해(빈 phase 목록, 이름·step·완료 확인 명령 누락, 잘못된 상태 값)면 즉시 멈추고 사람을 부릅니다. 기존 loop-run 등 사용처와 격리돼 하위호환을 깨지 않습니다.
- **0.8.0** — `loop-build` 신설(멀티-phase 무인 빌드아웃, phase 별 서브에이전트 maker). 루프 상한 기본값 조정(회차 10→5, 시간 60→120분, 비용 $100→$500, 토큰 1M→5M). loop-run 문서 정정.
- **0.6.0** — 과거 별도 plugin 이던 무인 검증 루프(loop-engine)를 ai-ready 안으로 통합. `/loop-run`·`/loop-review`·`/loop-lessons` + `loop-checker`·`loop-lesson-synthesizer` 에이전트 + 결정론 채점 셸이 audit/apply 와 한 plugin 으로 배포됩니다. 하나만 설치하면 `audit→apply→verify` 전 주기를 씁니다.
- **0.5.0** — apply 가 문서를 외과적으로 유지보수(사실만 모으는 `--json` 모드 + AI 부분 수정 + 사람 큐레이션 보존). 통째 덮어쓰기 폐기.
- **0.4.0** — 사람이 인수한 문서 보호(자동 생성 서명 없으면 덮어쓰기 거부, `--force` 우회, 문서별 diff 확인 루프).
- **0.3.0** — `.ai-ready/config.json` 의 rubric 섹션으로 채점이 프로젝트 현실을 반영(통합 design 디렉터리를 ADR 로, springdoc 를 API 계약으로 인정 등).
- **0.2.0** — `.ai-ready/config.json` 으로 frontmatter 인식 INDEX 그룹화와 사용자 lazy-load 트리거 보존.

<details>
<summary>더 이전 버전</summary>

- **0.7.0** — 5관점 적대 리뷰 기반 보강. 채점 정직화(빈/스텁 문서 부분점수, half-credit 공식 정합), 무인 루프 안전(베이스 ref·빈 diff 거짓 PASS 가드, checker/synthesizer 쓰기 금지 명문화), 배포 위생(freshness 훅 경로 통일, LICENSE 추가), 스크립트 견고성(BOM·타입 가드 등). **채점값이 일부 달라져 추이 재기준이 필요합니다.**
- **0.6.1** — loop 스킬 3개의 `disable-model-invocation` 제거(모델 자동 트리거에도 노출). 기능 변화 없음.
- **0.4.2** — INDEX 생성이 `.gitignore` 를 존중(추적·미무시 `.md` 만 인덱싱, 비-git 디렉터리는 전체 walk 로 폴백).
- **0.4.1** — 생성 스크립트 전반 버그 수정 묶음(worktree 오염으로 모듈·문서가 부풀던 문제 등 30건).
- **0.1.2** — manifest 스키마 수정, 단일 모듈 프로젝트 평가·스캐폴드 분기, thin-index 인식, 추이 스파크라인, ANTIPATTERNS 클러스터링.

</details>

